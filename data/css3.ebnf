(* Generated by mend.w3c.css3 *)

(* Basic CSS rules based on: https://www.w3.org/TR/CSS21/grammar.html *)

stylesheet = S ( css-ruleset | css-at-rule | css-comment )* ;

css-ruleset = css-selector [ ',' S css-selector ]* '{' S css-assignments '}' S ;

css-at-rule = prop-AT-charset
	    | prop-AT-counter-style
	    | prop-AT-font-face
	    | prop-AT-font-feature-values
	    | prop-AT-import
	    | prop-AT-keyframes
	    | prop-AT-media
	    | prop-AT-namespace
	    | prop-AT-page
	    | prop-AT-supports
	    | prop-AT-viewport

css-comment = '/*' ( #"[^*]*" | #'[*][^/]' )* '*/' S ;

css-selector = css-simple-selector [ css-combinator css-simple-selector ]* ;

css-combinator = '+' S
               | '>' S
               | '~' S
               | rS ;

css-simple-selector = [ css-type | css-univ ]
                      [ css-hash | css-class | css-attr | css-psuedo | css-negate ]*
                    | [ css-hash | css-class | css-attr | css-psuedo | css-negate ]+ ;

css-ns-pre = [ IDENT | '*' ]? '|' ;
css-type   = [ css-ns-pre ]? IDENT ;
css-univ   = [ css-ns-pre ]? '*' ;
css-hash   = '#' IDENT ;
css-class  = '.' IDENT ;
css-attr   = '[' S [ css-ns-pre ]? IDENT S
             [
               [
                 '^=' |
                 '$=' |
                 '*=' |
                 '='  |
                 '~=' |
                 '|='
               ]
               S [ IDENT | STRING ] S
             ]?
             ']' ;
css-psuedo = ':' ':'? [ IDENT | css-func ] ;
css-func   = IDENT '(' S css-expr ')' S ;
css-expr   = [ [ '+' | '-' | NUMBER '%' | NUMBER | STRING | IDENT ] S ]+ ;
css-negate = ':not(' S [ css-type | css-univ | css-hash | css-class | css-attr | css-psuedo ] S ')' ;

(* TODO: these should be merged with common definitions *)
NAME        = #'[_A-Za-z0-9-]+' ;
IDENT       = #'-?[_A-Za-z][_A-Za-z0-9-]*' ;
STRING      = quoted-string ;
NUMBER      = any-number ;


(* Hand-crafted assignments sequence definition. *)

(* We start here if we only want to parse inline CSS properties. *)
css-assignments = S | css-declaration S (';' S css-declaration S)* (';' S)* ;

css-declaration = ( css-known-standard / css-unknown ) ( '' | '!important' S )
                | css-comment ;

css-unknown = #'[A-Za-z-]+' S ':' S ( prop-all | prop-unknown ) ;

(* prop-unknown = #'[^";{}()]+' S ; *)
prop-unknown = S ( nonprop-custom-ident |
                   nonprop-string S |
                   nonprop-number S |
                   nonprop-percentage S |
                   nonprop-hex-color S |
                   func-calc
                   (* '(' prop-unknown ')' S *)
                 )*
nonprop-all = prop-all ;

(* Recursive defintions *)

nonprop-group-rule-body = stylesheet ;
prop-group-rule-body = css-ruleset ;
nonprop-declaration-list = css-assignments ;


css-known-standard =
  (
    "align-content" S ":" S prop-align-content |
    "align-items" S ":" S prop-align-items |
    "align-self" S ":" S prop-align-self |
    "all" S ":" S prop-all |
    "animation" S ":" S prop-animation |
    "animation-delay" S ":" S prop-animation-delay |
    "animation-direction" S ":" S prop-animation-direction |
    "animation-duration" S ":" S prop-animation-duration |
    "animation-fill-mode" S ":" S prop-animation-fill-mode |
    "animation-iteration-count" S ":" S prop-animation-iteration-count |
    "animation-name" S ":" S prop-animation-name |
    "animation-play-state" S ":" S prop-animation-play-state |
    "animation-timing-function" S ":" S prop-animation-timing-function |
    "backface-visibility" S ":" S prop-backface-visibility |
    "background" S ":" S prop-background |
    "background-attachment" S ":" S prop-background-attachment |
    "background-blend-mode" S ":" S prop-background-blend-mode |
    "background-clip" S ":" S prop-background-clip |
    "background-color" S ":" S prop-background-color |
    "background-image" S ":" S prop-background-image |
    "background-origin" S ":" S prop-background-origin |
    "background-position" S ":" S prop-background-position |
    "background-repeat" S ":" S prop-background-repeat |
    "background-size" S ":" S prop-background-size |
    "block-size" S ":" S prop-block-size |
    "border" S ":" S prop-border |
    "border-block" S ":" S prop-border-block |
    "border-block-color" S ":" S prop-border-block-color |
    "border-block-end" S ":" S prop-border-block-end |
    "border-block-end-color" S ":" S prop-border-block-end-color |
    "border-block-end-style" S ":" S prop-border-block-end-style |
    "border-block-end-width" S ":" S prop-border-block-end-width |
    "border-block-start" S ":" S prop-border-block-start |
    "border-block-start-color" S ":" S prop-border-block-start-color |
    "border-block-start-style" S ":" S prop-border-block-start-style |
    "border-block-start-width" S ":" S prop-border-block-start-width |
    "border-block-style" S ":" S prop-border-block-style |
    "border-block-width" S ":" S prop-border-block-width |
    "border-bottom" S ":" S prop-border-bottom |
    "border-bottom-color" S ":" S prop-border-bottom-color |
    "border-bottom-left-radius" S ":" S prop-border-bottom-left-radius |
    "border-bottom-right-radius" S ":" S prop-border-bottom-right-radius |
    "border-bottom-style" S ":" S prop-border-bottom-style |
    "border-bottom-width" S ":" S prop-border-bottom-width |
    "border-collapse" S ":" S prop-border-collapse |
    "border-color" S ":" S prop-border-color |
    "border-end-end-radius" S ":" S prop-border-end-end-radius |
    "border-end-start-radius" S ":" S prop-border-end-start-radius |
    "border-image" S ":" S prop-border-image |
    "border-image-outset" S ":" S prop-border-image-outset |
    "border-image-repeat" S ":" S prop-border-image-repeat |
    "border-image-slice" S ":" S prop-border-image-slice |
    "border-image-source" S ":" S prop-border-image-source |
    "border-image-width" S ":" S prop-border-image-width |
    "border-inline" S ":" S prop-border-inline |
    "border-inline-color" S ":" S prop-border-inline-color |
    "border-inline-end" S ":" S prop-border-inline-end |
    "border-inline-end-color" S ":" S prop-border-inline-end-color |
    "border-inline-end-style" S ":" S prop-border-inline-end-style |
    "border-inline-end-width" S ":" S prop-border-inline-end-width |
    "border-inline-start" S ":" S prop-border-inline-start |
    "border-inline-start-color" S ":" S prop-border-inline-start-color |
    "border-inline-start-style" S ":" S prop-border-inline-start-style |
    "border-inline-start-width" S ":" S prop-border-inline-start-width |
    "border-inline-style" S ":" S prop-border-inline-style |
    "border-inline-width" S ":" S prop-border-inline-width |
    "border-left" S ":" S prop-border-left |
    "border-left-color" S ":" S prop-border-left-color |
    "border-left-style" S ":" S prop-border-left-style |
    "border-left-width" S ":" S prop-border-left-width |
    "border-radius" S ":" S prop-border-radius |
    "border-right" S ":" S prop-border-right |
    "border-right-color" S ":" S prop-border-right-color |
    "border-right-style" S ":" S prop-border-right-style |
    "border-right-width" S ":" S prop-border-right-width |
    "border-spacing" S ":" S prop-border-spacing |
    "border-start-end-radius" S ":" S prop-border-start-end-radius |
    "border-start-start-radius" S ":" S prop-border-start-start-radius |
    "border-style" S ":" S prop-border-style |
    "border-top" S ":" S prop-border-top |
    "border-top-color" S ":" S prop-border-top-color |
    "border-top-left-radius" S ":" S prop-border-top-left-radius |
    "border-top-right-radius" S ":" S prop-border-top-right-radius |
    "border-top-style" S ":" S prop-border-top-style |
    "border-top-width" S ":" S prop-border-top-width |
    "border-width" S ":" S prop-border-width |
    "bottom" S ":" S prop-bottom |
    "box-decoration-break" S ":" S prop-box-decoration-break |
    "box-shadow" S ":" S prop-box-shadow |
    "box-sizing" S ":" S prop-box-sizing |
    "break-after" S ":" S prop-break-after |
    "break-before" S ":" S prop-break-before |
    "break-inside" S ":" S prop-break-inside |
    "caption-side" S ":" S prop-caption-side |
    "caret-color" S ":" S prop-caret-color |
    "clear" S ":" S prop-clear |
    "clip" S ":" S prop-clip |
    "clip-path" S ":" S prop-clip-path |
    "color" S ":" S prop-color |
    "color-adjust" S ":" S prop-color-adjust |
    "column-count" S ":" S prop-column-count |
    "column-fill" S ":" S prop-column-fill |
    "column-gap" S ":" S prop-column-gap |
    "column-rule" S ":" S prop-column-rule |
    "column-rule-color" S ":" S prop-column-rule-color |
    "column-rule-style" S ":" S prop-column-rule-style |
    "column-rule-width" S ":" S prop-column-rule-width |
    "column-span" S ":" S prop-column-span |
    "column-width" S ":" S prop-column-width |
    "columns" S ":" S prop-columns |
    "content" S ":" S prop-content |
    "counter-increment" S ":" S prop-counter-increment |
    "counter-reset" S ":" S prop-counter-reset |
    "cursor" S ":" S prop-cursor |
    "direction" S ":" S prop-direction |
    "display" S ":" S prop-display |
    "empty-cells" S ":" S prop-empty-cells |
    "filter" S ":" S prop-filter |
    "flex" S ":" S prop-flex |
    "flex-basis" S ":" S prop-flex-basis |
    "flex-direction" S ":" S prop-flex-direction |
    "flex-flow" S ":" S prop-flex-flow |
    "flex-grow" S ":" S prop-flex-grow |
    "flex-shrink" S ":" S prop-flex-shrink |
    "flex-wrap" S ":" S prop-flex-wrap |
    "float" S ":" S prop-float |
    "font" S ":" S prop-font |
    "font-family" S ":" S prop-font-family |
    "font-feature-settings" S ":" S prop-font-feature-settings |
    "font-kerning" S ":" S prop-font-kerning |
    "font-language-override" S ":" S prop-font-language-override |
    "font-optical-sizing" S ":" S prop-font-optical-sizing |
    "font-size" S ":" S prop-font-size |
    "font-size-adjust" S ":" S prop-font-size-adjust |
    "font-stretch" S ":" S prop-font-stretch |
    "font-style" S ":" S prop-font-style |
    "font-synthesis" S ":" S prop-font-synthesis |
    "font-variant" S ":" S prop-font-variant |
    "font-variant-alternates" S ":" S prop-font-variant-alternates |
    "font-variant-caps" S ":" S prop-font-variant-caps |
    "font-variant-east-asian" S ":" S prop-font-variant-east-asian |
    "font-variant-ligatures" S ":" S prop-font-variant-ligatures |
    "font-variant-numeric" S ":" S prop-font-variant-numeric |
    "font-variant-position" S ":" S prop-font-variant-position |
    "font-weight" S ":" S prop-font-weight |
    "gap" S ":" S prop-gap |
    "grid" S ":" S prop-grid |
    "grid-area" S ":" S prop-grid-area |
    "grid-auto-columns" S ":" S prop-grid-auto-columns |
    "grid-auto-flow" S ":" S prop-grid-auto-flow |
    "grid-auto-rows" S ":" S prop-grid-auto-rows |
    "grid-column" S ":" S prop-grid-column |
    "grid-column-end" S ":" S prop-grid-column-end |
    "grid-column-start" S ":" S prop-grid-column-start |
    "grid-row" S ":" S prop-grid-row |
    "grid-row-end" S ":" S prop-grid-row-end |
    "grid-row-start" S ":" S prop-grid-row-start |
    "grid-template" S ":" S prop-grid-template |
    "grid-template-areas" S ":" S prop-grid-template-areas |
    "grid-template-columns" S ":" S prop-grid-template-columns |
    "grid-template-rows" S ":" S prop-grid-template-rows |
    "hanging-punctuation" S ":" S prop-hanging-punctuation |
    "height" S ":" S prop-height |
    "hyphens" S ":" S prop-hyphens |
    "image-orientation" S ":" S prop-image-orientation |
    "image-rendering" S ":" S prop-image-rendering |
    "inline-size" S ":" S prop-inline-size |
    "inset" S ":" S prop-inset |
    "inset-block" S ":" S prop-inset-block |
    "inset-block-end" S ":" S prop-inset-block-end |
    "inset-block-start" S ":" S prop-inset-block-start |
    "inset-inline" S ":" S prop-inset-inline |
    "inset-inline-end" S ":" S prop-inset-inline-end |
    "inset-inline-start" S ":" S prop-inset-inline-start |
    "isolation" S ":" S prop-isolation |
    "justify-content" S ":" S prop-justify-content |
    "justify-items" S ":" S prop-justify-items |
    "justify-self" S ":" S prop-justify-self |
    "left" S ":" S prop-left |
    "letter-spacing" S ":" S prop-letter-spacing |
    "line-break" S ":" S prop-line-break |
    "line-height" S ":" S prop-line-height |
    "list-style" S ":" S prop-list-style |
    "list-style-image" S ":" S prop-list-style-image |
    "list-style-position" S ":" S prop-list-style-position |
    "list-style-type" S ":" S prop-list-style-type |
    "margin" S ":" S prop-margin |
    "margin-block" S ":" S prop-margin-block |
    "margin-block-end" S ":" S prop-margin-block-end |
    "margin-block-start" S ":" S prop-margin-block-start |
    "margin-bottom" S ":" S prop-margin-bottom |
    "margin-inline" S ":" S prop-margin-inline |
    "margin-inline-end" S ":" S prop-margin-inline-end |
    "margin-inline-start" S ":" S prop-margin-inline-start |
    "margin-left" S ":" S prop-margin-left |
    "margin-right" S ":" S prop-margin-right |
    "margin-top" S ":" S prop-margin-top |
    "mask" S ":" S prop-mask |
    "mask-clip" S ":" S prop-mask-clip |
    "mask-composite" S ":" S prop-mask-composite |
    "mask-image" S ":" S prop-mask-image |
    "mask-mode" S ":" S prop-mask-mode |
    "mask-origin" S ":" S prop-mask-origin |
    "mask-position" S ":" S prop-mask-position |
    "mask-repeat" S ":" S prop-mask-repeat |
    "mask-size" S ":" S prop-mask-size |
    "mask-type" S ":" S prop-mask-type |
    "max-height" S ":" S prop-max-height |
    "max-width" S ":" S prop-max-width |
    "min-block-size" S ":" S prop-min-block-size |
    "min-height" S ":" S prop-min-height |
    "min-inline-size" S ":" S prop-min-inline-size |
    "min-width" S ":" S prop-min-width |
    "mix-blend-mode" S ":" S prop-mix-blend-mode |
    "object-fit" S ":" S prop-object-fit |
    "object-position" S ":" S prop-object-position |
    "opacity" S ":" S prop-opacity |
    "order" S ":" S prop-order |
    "orphans" S ":" S prop-orphans |
    "outline" S ":" S prop-outline |
    "outline-color" S ":" S prop-outline-color |
    "outline-offset" S ":" S prop-outline-offset |
    "outline-style" S ":" S prop-outline-style |
    "outline-width" S ":" S prop-outline-width |
    "overflow" S ":" S prop-overflow |
    "overflow-wrap" S ":" S prop-overflow-wrap |
    "overflow-x" S ":" S prop-overflow-x |
    "overflow-y" S ":" S prop-overflow-y |
    "padding" S ":" S prop-padding |
    "padding-block" S ":" S prop-padding-block |
    "padding-block-end" S ":" S prop-padding-block-end |
    "padding-block-start" S ":" S prop-padding-block-start |
    "padding-bottom" S ":" S prop-padding-bottom |
    "padding-inline" S ":" S prop-padding-inline |
    "padding-inline-end" S ":" S prop-padding-inline-end |
    "padding-inline-start" S ":" S prop-padding-inline-start |
    "padding-left" S ":" S prop-padding-left |
    "padding-right" S ":" S prop-padding-right |
    "padding-top" S ":" S prop-padding-top |
    "page-break-after" S ":" S prop-page-break-after |
    "page-break-before" S ":" S prop-page-break-before |
    "page-break-inside" S ":" S prop-page-break-inside |
    "perspective" S ":" S prop-perspective |
    "perspective-origin" S ":" S prop-perspective-origin |
    "place-content" S ":" S prop-place-content |
    "place-items" S ":" S prop-place-items |
    "place-self" S ":" S prop-place-self |
    "pointer-events" S ":" S prop-pointer-events |
    "position" S ":" S prop-position |
    "quotes" S ":" S prop-quotes |
    "resize" S ":" S prop-resize |
    "right" S ":" S prop-right |
    "rotate" S ":" S prop-rotate |
    "row-gap" S ":" S prop-row-gap |
    "scale" S ":" S prop-scale |
    "scroll-behavior" S ":" S prop-scroll-behavior |
    "scroll-margin" S ":" S prop-scroll-margin |
    "scroll-margin-block" S ":" S prop-scroll-margin-block |
    "scroll-margin-block-end" S ":" S prop-scroll-margin-block-end |
    "scroll-margin-block-start" S ":" S prop-scroll-margin-block-start |
    "scroll-margin-bottom" S ":" S prop-scroll-margin-bottom |
    "scroll-margin-inline" S ":" S prop-scroll-margin-inline |
    "scroll-margin-inline-end" S ":" S prop-scroll-margin-inline-end |
    "scroll-margin-inline-start" S ":" S prop-scroll-margin-inline-start |
    "scroll-margin-left" S ":" S prop-scroll-margin-left |
    "scroll-margin-right" S ":" S prop-scroll-margin-right |
    "scroll-margin-top" S ":" S prop-scroll-margin-top |
    "scroll-padding" S ":" S prop-scroll-padding |
    "scroll-padding-block" S ":" S prop-scroll-padding-block |
    "scroll-padding-block-end" S ":" S prop-scroll-padding-block-end |
    "scroll-padding-block-start" S ":" S prop-scroll-padding-block-start |
    "scroll-padding-bottom" S ":" S prop-scroll-padding-bottom |
    "scroll-padding-inline" S ":" S prop-scroll-padding-inline |
    "scroll-padding-inline-end" S ":" S prop-scroll-padding-inline-end |
    "scroll-padding-inline-start" S ":" S prop-scroll-padding-inline-start |
    "scroll-padding-left" S ":" S prop-scroll-padding-left |
    "scroll-padding-right" S ":" S prop-scroll-padding-right |
    "scroll-padding-top" S ":" S prop-scroll-padding-top |
    "scroll-snap-align" S ":" S prop-scroll-snap-align |
    "scroll-snap-stop" S ":" S prop-scroll-snap-stop |
    "scroll-snap-type" S ":" S prop-scroll-snap-type |
    "scrollbar-color" S ":" S prop-scrollbar-color |
    "scrollbar-width" S ":" S prop-scrollbar-width |
    "shape-image-threshold" S ":" S prop-shape-image-threshold |
    "shape-margin" S ":" S prop-shape-margin |
    "shape-outside" S ":" S prop-shape-outside |
    "tab-size" S ":" S prop-tab-size |
    "table-layout" S ":" S prop-table-layout |
    "text-align" S ":" S prop-text-align |
    "text-align-last" S ":" S prop-text-align-last |
    "text-combine-upright" S ":" S prop-text-combine-upright |
    "text-decoration" S ":" S prop-text-decoration |
    "text-decoration-color" S ":" S prop-text-decoration-color |
    "text-decoration-line" S ":" S prop-text-decoration-line |
    "text-decoration-style" S ":" S prop-text-decoration-style |
    "text-emphasis" S ":" S prop-text-emphasis |
    "text-emphasis-color" S ":" S prop-text-emphasis-color |
    "text-emphasis-position" S ":" S prop-text-emphasis-position |
    "text-emphasis-style" S ":" S prop-text-emphasis-style |
    "text-indent" S ":" S prop-text-indent |
    "text-justify" S ":" S prop-text-justify |
    "text-orientation" S ":" S prop-text-orientation |
    "text-overflow" S ":" S prop-text-overflow |
    "text-rendering" S ":" S prop-text-rendering |
    "text-shadow" S ":" S prop-text-shadow |
    "text-transform" S ":" S prop-text-transform |
    "text-underline-position" S ":" S prop-text-underline-position |
    "top" S ":" S prop-top |
    "touch-action" S ":" S prop-touch-action |
    "transform" S ":" S prop-transform |
    "transform-box" S ":" S prop-transform-box |
    "transform-origin" S ":" S prop-transform-origin |
    "transform-style" S ":" S prop-transform-style |
    "transition" S ":" S prop-transition |
    "transition-delay" S ":" S prop-transition-delay |
    "transition-duration" S ":" S prop-transition-duration |
    "transition-property" S ":" S prop-transition-property |
    "transition-timing-function" S ":" S prop-transition-timing-function |
    "translate" S ":" S prop-translate |
    "unicode-bidi" S ":" S prop-unicode-bidi |
    "vertical-align" S ":" S prop-vertical-align |
    "visibility" S ":" S prop-visibility |
    "white-space" S ":" S prop-white-space |
    "widows" S ":" S prop-widows |
    "width" S ":" S prop-width |
    "will-change" S ":" S prop-will-change |
    "word-break" S ":" S prop-word-break |
    "word-spacing" S ":" S prop-word-spacing |
    "word-wrap" S ":" S prop-word-wrap |
    "writing-mode" S ":" S prop-writing-mode |
    "z-index" S ":" S prop-z-index
  ) ;

prop-AT-charset =
  (
    ('@charset' S)
    ('"' S)
    nonprop-charset
    ('"' S)
    (';' S)
  ) ;

prop-AT-counter-style =
  (
    ('@counter-style' S)
    nonprop-counter-style-name
    '{' S
      ( (
        (
          ('system:' S)
          nonprop-counter-system
          (';' S)
        ) |
        (
          ('symbols:' S)
          nonprop-counter-symbols
          (';' S)
        ) |
        (
          ('additive-symbols:' S)
          nonprop-additive-symbols
          (';' S)
        ) |
        (
          ('negative:' S)
          nonprop-negative-symbol
          (';' S)
        ) |
        (
          ('prefix:' S)
          nonprop-prefix
          (';' S)
        ) |
        (
          ('suffix:' S)
          nonprop-suffix
          (';' S)
        ) |
        (
          ('range:' S)
          nonprop-range
          (';' S)
        ) |
        (
          ('pad:' S)
          nonprop-padding
          (';' S)
        ) |
        (
          ('speak-as:' S)
          nonprop-speak-as
          (';' S)
        ) |
        (
          ('fallback:' S)
          nonprop-counter-style-name
          (';' S)
        )
      )+ )
    '}' S
  ) ;

prop-AT-font-face =
  (
    ('@font-face' S)
    '{' S
      ( (
        (
          ('font-family:' S)
          nonprop-family-name
          (';' S)
        ) |
        (
          ('src:' S)
          nonprop-src
          (';' S)
        ) |
        (
          ('unicode-range:' S)
          nonprop-unicode-range
          (';' S)
        ) |
        (
          ('font-variant:' S)
          nonprop-font-variant
          (';' S)
        ) |
        (
          ('font-feature-settings:' S)
          nonprop-font-feature-settings
          (';' S)
        ) |
        (
          ('font-variation-settings:' S)
          nonprop-font-variation-settings
          (';' S)
        ) |
        (
          ('font-stretch:' S)
          nonprop-font-stretch
          (';' S)
        ) |
        (
          ('font-weight:' S)
          nonprop-font-weight
          (';' S)
        ) |
        (
          ('font-style:' S)
          nonprop-font-style
          (';' S)
        ) |
        (
          ('font-display:' S)
          nonprop-font-display
          (';' S)
        )
      )+ )
    '}' S
  ) ;

prop-AT-font-feature-values =
  (
    ('@font-feature-values' S)
    nonprop-family-name (
      ',' S
      nonprop-family-name
    )*
    '{' S
      nonprop-feature-value-block-list
    '}' S
  ) ;

prop-AT-import =
  (
    ('@import' S)
    (
      nonprop-string |
      nonprop-url
    )
    nonprop-media-query-list?
    (';' S)
  ) ;

prop-AT-keyframes =
  (
    ('@keyframes' S)
    nonprop-keyframes-name
    '{' S
      nonprop-keyframe-block-list
    '}' S
  ) ;

prop-AT-media =
  (
    ('@media' S)
    nonprop-media-query-list
    '{' S
      nonprop-group-rule-body
    '}' S
  ) ;

prop-AT-namespace =
  (
    ('@namespace' S)
    nonprop-namespace-prefix?
    (
      nonprop-string |
      nonprop-url
    )
    (';' S)
  ) ;

prop-AT-page =
  (
    ('@page' S)
    nonprop-page-selector-list
    '{' S
      nonprop-page-body
    '}' S
  ) ;

prop-AT-supports =
  (
    ('@supports' S)
    nonprop-supports-condition
    '{' S
      nonprop-group-rule-body
    '}' S
  ) ;

prop-AT-viewport =
  (
    ('@viewport' S)
    '{' S
      nonprop-group-rule-body
    '}' S
  ) ;

prop-align-content =
  (
    prop-all |
    ('normal' S) |
    nonprop-baseline-position |
    nonprop-content-distribution |
    (
      nonprop-overflow-position?
      nonprop-content-position
    )
  ) ;

prop-align-items =
  (
    prop-all |
    ('normal' S) |
    ('stretch' S) |
    nonprop-baseline-position |
    (
      nonprop-overflow-position?
      nonprop-self-position
    )
  ) ;

prop-align-self =
  (
    prop-all |
    ('auto' S) |
    ('normal' S) |
    ('stretch' S) |
    nonprop-baseline-position |
    (
      nonprop-overflow-position?
      nonprop-self-position
    )
  ) ;

prop-all =
  (
    ('initial' S) |
    ('inherit' S) |
    ('unset' S) |
    ('revert' S)
  ) ;

prop-animation =
  (
    prop-all |
    nonprop-single-animation (
      ',' S
      nonprop-single-animation
    )*
  ) ;

prop-animation-delay =
  (
    prop-all |
    nonprop-time (
      ',' S
      nonprop-time
    )*
  ) ;

prop-animation-direction =
  (
    prop-all |
    nonprop-single-animation-direction (
      ',' S
      nonprop-single-animation-direction
    )*
  ) ;

prop-animation-duration =
  (
    prop-all |
    nonprop-time (
      ',' S
      nonprop-time
    )*
  ) ;

prop-animation-fill-mode =
  (
    prop-all |
    nonprop-single-animation-fill-mode (
      ',' S
      nonprop-single-animation-fill-mode
    )*
  ) ;

prop-animation-iteration-count =
  (
    prop-all |
    nonprop-single-animation-iteration-count (
      ',' S
      nonprop-single-animation-iteration-count
    )*
  ) ;

prop-animation-name =
  (
    prop-all |
    (
      ('none' S) |
      nonprop-keyframes-name
    ) (
      ',' S
      (
        ('none' S) |
        nonprop-keyframes-name
      )
    )*
  ) ;

prop-animation-play-state =
  (
    prop-all |
    nonprop-single-animation-play-state (
      ',' S
      nonprop-single-animation-play-state
    )*
  ) ;

prop-animation-timing-function =
  (
    prop-all |
    nonprop-timing-function (
      ',' S
      nonprop-timing-function
    )*
  ) ;

prop-backface-visibility =
  (
    prop-all |
    ('visible' S) |
    ('hidden' S)
  ) ;

prop-background =
  (
    prop-all |
    (
      (
        nonprop-bg-layer
        (',' S)
      )*
      nonprop-final-bg-layer
    )
  ) ;

prop-background-attachment =
  (
    prop-all |
    nonprop-attachment (
      ',' S
      nonprop-attachment
    )*
  ) ;

prop-background-blend-mode =
  (
    prop-all |
    nonprop-blend-mode (
      ',' S
      nonprop-blend-mode
    )*
  ) ;

prop-background-clip =
  (
    prop-all |
    nonprop-box (
      ',' S
      nonprop-box
    )*
  ) ;

prop-background-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-background-image =
  (
    prop-all |
    nonprop-bg-image (
      ',' S
      nonprop-bg-image
    )*
  ) ;

prop-background-origin =
  (
    prop-all |
    nonprop-box (
      ',' S
      nonprop-box
    )*
  ) ;

prop-background-position =
  (
    prop-all |
    nonprop-bg-position (
      ',' S
      nonprop-bg-position
    )*
  ) ;

prop-background-repeat =
  (
    prop-all |
    nonprop-repeat-style (
      ',' S
      nonprop-repeat-style
    )*
  ) ;

prop-background-size =
  (
    prop-all |
    nonprop-bg-size (
      ',' S
      nonprop-bg-size
    )*
  ) ;

prop-block-size =
  (
    prop-all |
    prop-width
  ) ;

prop-border =
  (
    prop-all |
    ( (
      nonprop-line-width |
      nonprop-line-style |
      nonprop-color
    )+ )
  ) ;

prop-border-block =
  (
    prop-all |
    ( (
      prop-border-top-width |
      prop-border-top-style |
      prop-color
    )+ )
  ) ;

prop-border-block-color =
  (
    prop-all |
    (
      (
        prop-border-top-color |
        prop-border-top-color rS
        prop-border-top-color
      )
    )
  ) ;

prop-border-block-end =
  (
    prop-all |
    ( (
      prop-border-top-width |
      prop-border-top-style |
      prop-color
    )+ )
  ) ;

prop-border-block-end-color =
  (
    prop-all |
    prop-border-top-color
  ) ;

prop-border-block-end-style =
  (
    prop-all |
    prop-border-top-style
  ) ;

prop-border-block-end-width =
  (
    prop-all |
    prop-border-top-width
  ) ;

prop-border-block-start =
  (
    prop-all |
    ( (
      prop-border-top-width |
      prop-border-top-style |
      prop-color
    )+ )
  ) ;

prop-border-block-start-color =
  (
    prop-all |
    prop-border-top-color
  ) ;

prop-border-block-start-style =
  (
    prop-all |
    prop-border-top-style
  ) ;

prop-border-block-start-width =
  (
    prop-all |
    prop-border-top-width
  ) ;

prop-border-block-style =
  (
    prop-all |
    prop-border-top-style
  ) ;

prop-border-block-width =
  (
    prop-all |
    prop-border-top-width
  ) ;

prop-border-bottom =
  (
    prop-all |
    ( (
      nonprop-line-width |
      nonprop-line-style |
      nonprop-color
    )+ )
  ) ;

prop-border-bottom-color =
  (
    prop-all |
    prop-border-top-color
  ) ;

prop-border-bottom-left-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-bottom-right-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-bottom-style =
  (
    prop-all |
    nonprop-line-style
  ) ;

prop-border-bottom-width =
  (
    prop-all |
    nonprop-line-width
  ) ;

prop-border-collapse =
  (
    prop-all |
    ('collapse' S) |
    ('separate' S)
  ) ;

prop-border-color =
  (
    prop-all |
    (
      (
        nonprop-color |
        nonprop-color rS
        nonprop-color |
        nonprop-color rS
        nonprop-color rS
        nonprop-color |
        nonprop-color rS
        nonprop-color rS
        nonprop-color rS
        nonprop-color
      )
    )
  ) ;

prop-border-end-end-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-end-start-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-image =
  (
    prop-all |
    ( (
      prop-border-image-source |
      (
        prop-border-image-slice
        (
          (
            ('/' S)
            prop-border-image-width
          ) |
          (
            ('/' S)
            prop-border-image-width?
            ('/' S)
            prop-border-image-outset
          )
        )?
      ) |
      prop-border-image-repeat
    )+ )
  ) ;

prop-border-image-outset =
  (
    prop-all |
    (
      (
        (
          nonprop-length |
          nonprop-number
        ) |
        (
          nonprop-length |
          nonprop-number
        ) rS
        (
          nonprop-length |
          nonprop-number
        ) |
        (
          nonprop-length |
          nonprop-number
        ) rS
        (
          nonprop-length |
          nonprop-number
        ) rS
        (
          nonprop-length |
          nonprop-number
        ) |
        (
          nonprop-length |
          nonprop-number
        ) rS
        (
          nonprop-length |
          nonprop-number
        ) rS
        (
          nonprop-length |
          nonprop-number
        ) rS
        (
          nonprop-length |
          nonprop-number
        )
      )
    )
  ) ;

prop-border-image-repeat =
  (
    prop-all |
    (
      (
        (
          ('stretch' S) |
          ('repeat' S) |
          ('round' S) |
          ('space' S)
        ) |
        (
          ('stretch' S) |
          ('repeat' S) |
          ('round' S) |
          ('space' S)
        ) rS
        (
          ('stretch' S) |
          ('repeat' S) |
          ('round' S) |
          ('space' S)
        )
      )
    )
  ) ;

prop-border-image-slice =
  (
    prop-all |
    (
      (
        (
          nonprop-number-percentage |
          nonprop-number-percentage rS
          nonprop-number-percentage |
          nonprop-number-percentage rS
          nonprop-number-percentage rS
          nonprop-number-percentage |
          nonprop-number-percentage rS
          nonprop-number-percentage rS
          nonprop-number-percentage rS
          nonprop-number-percentage
        )
      )
      ('fill' S)?
    )
  ) ;

prop-border-image-source =
  (
    prop-all |
    ('none' S) |
    nonprop-image
  ) ;

prop-border-image-width =
  (
    prop-all |
    (
      (
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) |
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) |
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) |
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          nonprop-number |
          ('auto' S)
        )
      )
    )
  ) ;

prop-border-inline =
  (
    prop-all |
    ( (
      prop-border-top-width |
      prop-border-top-style |
      prop-color
    )+ )
  ) ;

prop-border-inline-color =
  (
    prop-all |
    (
      (
        prop-border-top-color |
        prop-border-top-color rS
        prop-border-top-color
      )
    )
  ) ;

prop-border-inline-end =
  (
    prop-all |
    ( (
      prop-border-top-width |
      prop-border-top-style |
      prop-color
    )+ )
  ) ;

prop-border-inline-end-color =
  (
    prop-all |
    prop-border-top-color
  ) ;

prop-border-inline-end-style =
  (
    prop-all |
    prop-border-top-style
  ) ;

prop-border-inline-end-width =
  (
    prop-all |
    prop-border-top-width
  ) ;

prop-border-inline-start =
  (
    prop-all |
    ( (
      prop-border-top-width |
      prop-border-top-style |
      prop-color
    )+ )
  ) ;

prop-border-inline-start-color =
  (
    prop-all |
    prop-border-top-color
  ) ;

prop-border-inline-start-style =
  (
    prop-all |
    prop-border-top-style
  ) ;

prop-border-inline-start-width =
  (
    prop-all |
    prop-border-top-width
  ) ;

prop-border-inline-style =
  (
    prop-all |
    prop-border-top-style
  ) ;

prop-border-inline-width =
  (
    prop-all |
    prop-border-top-width
  ) ;

prop-border-left =
  (
    prop-all |
    ( (
      nonprop-line-width |
      nonprop-line-style |
      nonprop-color
    )+ )
  ) ;

prop-border-left-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-border-left-style =
  (
    prop-all |
    nonprop-line-style
  ) ;

prop-border-left-width =
  (
    prop-all |
    nonprop-line-width
  ) ;

prop-border-radius =
  (
    prop-all |
    (
      (
        (
          nonprop-length-percentage |
          nonprop-length-percentage rS
          nonprop-length-percentage |
          nonprop-length-percentage rS
          nonprop-length-percentage rS
          nonprop-length-percentage |
          nonprop-length-percentage rS
          nonprop-length-percentage rS
          nonprop-length-percentage rS
          nonprop-length-percentage
        )
      )
      (
        ('/' S)
        (
          (
            nonprop-length-percentage |
            nonprop-length-percentage rS
            nonprop-length-percentage |
            nonprop-length-percentage rS
            nonprop-length-percentage rS
            nonprop-length-percentage |
            nonprop-length-percentage rS
            nonprop-length-percentage rS
            nonprop-length-percentage rS
            nonprop-length-percentage
          )
        )
      )?
    )
  ) ;

prop-border-right =
  (
    prop-all |
    ( (
      nonprop-line-width |
      nonprop-line-style |
      nonprop-color
    )+ )
  ) ;

prop-border-right-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-border-right-style =
  (
    prop-all |
    nonprop-line-style
  ) ;

prop-border-right-width =
  (
    prop-all |
    nonprop-line-width
  ) ;

prop-border-spacing =
  (
    prop-all |
    (
      nonprop-length
      nonprop-length?
    )
  ) ;

prop-border-start-end-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-start-start-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-style =
  (
    prop-all |
    (
      (
        nonprop-line-style |
        nonprop-line-style rS
        nonprop-line-style |
        nonprop-line-style rS
        nonprop-line-style rS
        nonprop-line-style |
        nonprop-line-style rS
        nonprop-line-style rS
        nonprop-line-style rS
        nonprop-line-style
      )
    )
  ) ;

prop-border-top =
  (
    prop-all |
    ( (
      nonprop-line-width |
      nonprop-line-style |
      nonprop-color
    )+ )
  ) ;

prop-border-top-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-border-top-left-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-top-right-radius =
  (
    prop-all |
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

prop-border-top-style =
  (
    prop-all |
    nonprop-line-style
  ) ;

prop-border-top-width =
  (
    prop-all |
    nonprop-line-width
  ) ;

prop-border-width =
  (
    prop-all |
    (
      (
        nonprop-line-width |
        nonprop-line-width rS
        nonprop-line-width |
        nonprop-line-width rS
        nonprop-line-width rS
        nonprop-line-width |
        nonprop-line-width rS
        nonprop-line-width rS
        nonprop-line-width rS
        nonprop-line-width
      )
    )
  ) ;

prop-bottom =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-box-decoration-break =
  (
    prop-all |
    ('slice' S) |
    ('clone' S)
  ) ;

prop-box-shadow =
  (
    prop-all |
    ('none' S) |
    nonprop-shadow (
      ',' S
      nonprop-shadow
    )*
  ) ;

prop-box-sizing =
  (
    prop-all |
    ('content-box' S) |
    ('border-box' S)
  ) ;

prop-break-after =
  (
    prop-all |
    ('auto' S) |
    ('avoid' S) |
    ('always' S) |
    ('all' S) |
    ('avoid-page' S) |
    ('page' S) |
    ('left' S) |
    ('right' S) |
    ('recto' S) |
    ('verso' S) |
    ('avoid-column' S) |
    ('column' S) |
    ('avoid-region' S) |
    ('region' S)
  ) ;

prop-break-before =
  (
    prop-all |
    ('auto' S) |
    ('avoid' S) |
    ('always' S) |
    ('all' S) |
    ('avoid-page' S) |
    ('page' S) |
    ('left' S) |
    ('right' S) |
    ('recto' S) |
    ('verso' S) |
    ('avoid-column' S) |
    ('column' S) |
    ('avoid-region' S) |
    ('region' S)
  ) ;

prop-break-inside =
  (
    prop-all |
    ('auto' S) |
    ('avoid' S) |
    ('avoid-page' S) |
    ('avoid-column' S) |
    ('avoid-region' S)
  ) ;

prop-caption-side =
  (
    prop-all |
    ('top' S) |
    ('bottom' S) |
    ('block-start' S) |
    ('block-end' S) |
    ('inline-start' S) |
    ('inline-end' S)
  ) ;

prop-caret-color =
  (
    prop-all |
    ('auto' S) |
    nonprop-color
  ) ;

prop-clear =
  (
    prop-all |
    ('none' S) |
    ('left' S) |
    ('right' S) |
    ('both' S) |
    ('inline-start' S) |
    ('inline-end' S)
  ) ;

prop-clip =
  (
    prop-all |
    nonprop-shape |
    ('auto' S)
  ) ;

prop-clip-path =
  (
    prop-all |
    nonprop-clip-source |
    ( (
      nonprop-basic-shape |
      nonprop-geometry-box
    )+ ) |
    ('none' S)
  ) ;

prop-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-color-adjust =
  (
    prop-all |
    ('economy' S) |
    ('exact' S)
  ) ;

prop-column-count =
  (
    prop-all |
    nonprop-integer |
    ('auto' S)
  ) ;

prop-column-fill =
  (
    prop-all |
    ('auto' S) |
    ('balance' S) |
    ('balance-all' S)
  ) ;

prop-column-gap =
  (
    prop-all |
    ('normal' S) |
    nonprop-length-percentage
  ) ;

prop-column-rule =
  (
    prop-all |
    ( (
      prop-column-rule-width |
      prop-column-rule-style |
      prop-column-rule-color
    )+ )
  ) ;

prop-column-rule-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-column-rule-style =
  (
    prop-all |
    prop-border-style
  ) ;

prop-column-rule-width =
  (
    prop-all |
    prop-border-width
  ) ;

prop-column-span =
  (
    prop-all |
    ('none' S) |
    ('all' S)
  ) ;

prop-column-width =
  (
    prop-all |
    nonprop-length |
    ('auto' S)
  ) ;

prop-columns =
  (
    prop-all |
    ( (
      prop-column-width |
      prop-column-count
    )+ )
  ) ;

prop-content =
  (
    prop-all |
    ('normal' S) |
    ('none' S) |
    (
      (
        nonprop-content-replacement |
        nonprop-content-list
      )
      (
        ('/' S)
        nonprop-string
      )?
    )
  ) ;

prop-counter-increment =
  (
    prop-all |
    (
      nonprop-custom-ident
      nonprop-integer?
    )+ |
    ('none' S)
  ) ;

prop-counter-reset =
  (
    prop-all |
    (
      nonprop-custom-ident
      nonprop-integer?
    )+ |
    ('none' S)
  ) ;

prop-cursor =
  (
    prop-all |
    (
      (
        nonprop-url
        (
          nonprop-x
          nonprop-y
        )?
        (',' S)
      )*
      (
        ('auto' S) |
        ('default' S) |
        ('none' S) |
        ('context-menu' S) |
        ('help' S) |
        ('pointer' S) |
        ('progress' S) |
        ('wait' S) |
        ('cell' S) |
        ('crosshair' S) |
        ('text' S) |
        ('vertical-text' S) |
        ('alias' S) |
        ('copy' S) |
        ('move' S) |
        ('no-drop' S) |
        ('not-allowed' S) |
        ('e-resize' S) |
        ('n-resize' S) |
        ('ne-resize' S) |
        ('nw-resize' S) |
        ('s-resize' S) |
        ('se-resize' S) |
        ('sw-resize' S) |
        ('w-resize' S) |
        ('ew-resize' S) |
        ('ns-resize' S) |
        ('nesw-resize' S) |
        ('nwse-resize' S) |
        ('col-resize' S) |
        ('row-resize' S) |
        ('all-scroll' S) |
        ('zoom-in' S) |
        ('zoom-out' S) |
        ('grab' S) |
        ('grabbing' S)
      )
    )
  ) ;

prop-direction =
  (
    prop-all |
    ('ltr' S) |
    ('rtl' S)
  ) ;

prop-display =
  (
    prop-all |
    ( (
      nonprop-display-outside |
      nonprop-display-inside
    )+ ) |
    nonprop-display-listitem |
    nonprop-display-internal |
    nonprop-display-box |
    nonprop-display-legacy
  ) ;

prop-empty-cells =
  (
    prop-all |
    ('show' S) |
    ('hide' S)
  ) ;

prop-filter =
  (
    prop-all |
    ('none' S) |
    nonprop-filter-function-list
  ) ;

prop-flex =
  (
    prop-all |
    ('none' S) |
    ( (
      (
        prop-flex-grow
        prop-flex-shrink?
      ) |
      prop-flex-basis
    )+ )
  ) ;

prop-flex-basis =
  (
    prop-all |
    ('content' S) |
    prop-width
  ) ;

prop-flex-direction =
  (
    prop-all |
    ('row' S) |
    ('row-reverse' S) |
    ('column' S) |
    ('column-reverse' S)
  ) ;

prop-flex-flow =
  (
    prop-all |
    ( (
      prop-flex-direction |
      prop-flex-wrap
    )+ )
  ) ;

prop-flex-grow =
  (
    prop-all |
    nonprop-number
  ) ;

prop-flex-shrink =
  (
    prop-all |
    nonprop-number
  ) ;

prop-flex-wrap =
  (
    prop-all |
    ('nowrap' S) |
    ('wrap' S) |
    ('wrap-reverse' S)
  ) ;

prop-float =
  (
    prop-all |
    ('left' S) |
    ('right' S) |
    ('none' S) |
    ('inline-start' S) |
    ('inline-end' S)
  ) ;

prop-font =
  (
    prop-all |
    (
      ( (
        prop-font-style |
        nonprop-font-variant-css21 |
        prop-font-weight |
        prop-font-stretch
      )+ )?
      prop-font-size
      (
        ('/' S)
        prop-line-height
      )?
      prop-font-family
    ) |
    ('caption' S) |
    ('icon' S) |
    ('menu' S) |
    ('message-box' S) |
    ('small-caption' S) |
    ('status-bar' S)
  ) ;

prop-font-family =
  (
    prop-all |
    (
      nonprop-family-name |
      nonprop-generic-family
    ) (
      ',' S
      (
        nonprop-family-name |
        nonprop-generic-family
      )
    )*
  ) ;

prop-font-feature-settings =
  (
    prop-all |
    ('normal' S) |
    nonprop-feature-tag-value (
      ',' S
      nonprop-feature-tag-value
    )*
  ) ;

prop-font-kerning =
  (
    prop-all |
    ('auto' S) |
    ('normal' S) |
    ('none' S)
  ) ;

prop-font-language-override =
  (
    prop-all |
    ('normal' S) |
    nonprop-string
  ) ;

prop-font-optical-sizing =
  (
    prop-all |
    ('auto' S) |
    ('none' S)
  ) ;

prop-font-size =
  (
    prop-all |
    nonprop-absolute-size |
    nonprop-relative-size |
    nonprop-length-percentage
  ) ;

prop-font-size-adjust =
  (
    prop-all |
    ('none' S) |
    nonprop-number
  ) ;

prop-font-stretch =
  (
    prop-all |
    nonprop-font-stretch-absolute
  ) ;

prop-font-style =
  (
    prop-all |
    ('normal' S) |
    ('italic' S) |
    (
      ('oblique' S)
      nonprop-angle?
    )
  ) ;

prop-font-synthesis =
  (
    prop-all |
    ('none' S) |
    ( (
      ('weight' S) |
      ('style' S)
    )+ )
  ) ;

prop-font-variant =
  (
    prop-all |
    ('normal' S) |
    ('none' S) |
    ( (
      nonprop-common-lig-values |
      nonprop-discretionary-lig-values |
      nonprop-historical-lig-values |
      nonprop-contextual-alt-values |
      (
        ('stylistic' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      ('historical-forms' S) |
      (
        ('styleset' S)
        ('(' S)
        nonprop-feature-value-name (
          ',' S
          nonprop-feature-value-name
        )*
        (')' S)
      ) |
      (
        ('character-variant' S)
        ('(' S)
        nonprop-feature-value-name (
          ',' S
          nonprop-feature-value-name
        )*
        (')' S)
      ) |
      (
        ('swash' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      (
        ('ornaments' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      (
        ('annotation' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      (
        ('small-caps' S) |
        ('all-small-caps' S) |
        ('petite-caps' S) |
        ('all-petite-caps' S) |
        ('unicase' S) |
        ('titling-caps' S)
      ) |
      nonprop-numeric-figure-values |
      nonprop-numeric-spacing-values |
      nonprop-numeric-fraction-values |
      ('ordinal' S) |
      ('slashed-zero' S) |
      nonprop-east-asian-variant-values |
      nonprop-east-asian-width-values |
      ('ruby' S)
    )+ )
  ) ;

prop-font-variant-alternates =
  (
    prop-all |
    ('normal' S) |
    ( (
      (
        ('stylistic' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      ('historical-forms' S) |
      (
        ('styleset' S)
        ('(' S)
        nonprop-feature-value-name (
          ',' S
          nonprop-feature-value-name
        )*
        (')' S)
      ) |
      (
        ('character-variant' S)
        ('(' S)
        nonprop-feature-value-name (
          ',' S
          nonprop-feature-value-name
        )*
        (')' S)
      ) |
      (
        ('swash' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      (
        ('ornaments' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      ) |
      (
        ('annotation' S)
        ('(' S)
        nonprop-feature-value-name
        (')' S)
      )
    )+ )
  ) ;

prop-font-variant-caps =
  (
    prop-all |
    ('normal' S) |
    ('small-caps' S) |
    ('all-small-caps' S) |
    ('petite-caps' S) |
    ('all-petite-caps' S) |
    ('unicase' S) |
    ('titling-caps' S)
  ) ;

prop-font-variant-east-asian =
  (
    prop-all |
    ('normal' S) |
    ( (
      nonprop-east-asian-variant-values |
      nonprop-east-asian-width-values |
      ('ruby' S)
    )+ )
  ) ;

prop-font-variant-ligatures =
  (
    prop-all |
    ('normal' S) |
    ('none' S) |
    ( (
      nonprop-common-lig-values |
      nonprop-discretionary-lig-values |
      nonprop-historical-lig-values |
      nonprop-contextual-alt-values
    )+ )
  ) ;

prop-font-variant-numeric =
  (
    prop-all |
    ('normal' S) |
    ( (
      nonprop-numeric-figure-values |
      nonprop-numeric-spacing-values |
      nonprop-numeric-fraction-values |
      ('ordinal' S) |
      ('slashed-zero' S)
    )+ )
  ) ;

prop-font-variant-position =
  (
    prop-all |
    ('normal' S) |
    ('sub' S) |
    ('super' S)
  ) ;

prop-font-weight =
  (
    prop-all |
    nonprop-font-weight-absolute |
    ('bolder' S) |
    ('lighter' S)
  ) ;

prop-gap =
  (
    prop-all |
    (
      prop-row-gap
      prop-column-gap?
    )
  ) ;

prop-grid =
  (
    prop-all |
    prop-grid-template |
    (
      prop-grid-template-rows
      ('/' S)
      (
        ('auto-flow' S)
        ('dense' S)?
      )
      prop-grid-auto-columns?
    ) |
    (
      (
        ('auto-flow' S)
        ('dense' S)?
      )
      prop-grid-auto-rows?
      ('/' S)
      prop-grid-template-columns
    )
  ) ;

prop-grid-area =
  (
    prop-all |
    (
      nonprop-grid-line
      (
        (
          '' |
          (
            ('/' S)
            nonprop-grid-line
          ) |
          (
            ('/' S)
            nonprop-grid-line
          ) rS
          (
            ('/' S)
            nonprop-grid-line
          ) |
          (
            ('/' S)
            nonprop-grid-line
          ) rS
          (
            ('/' S)
            nonprop-grid-line
          ) rS
          (
            ('/' S)
            nonprop-grid-line
          )
        )
      )
    )
  ) ;

prop-grid-auto-columns =
  (
    prop-all |
    nonprop-track-size+
  ) ;

prop-grid-auto-flow =
  (
    prop-all |
    ( (
      (
        ('row' S) |
        ('column' S)
      ) |
      ('dense' S)
    )+ )
  ) ;

prop-grid-auto-rows =
  (
    prop-all |
    nonprop-track-size+
  ) ;

prop-grid-column =
  (
    prop-all |
    (
      nonprop-grid-line
      (
        ('/' S)
        nonprop-grid-line
      )?
    )
  ) ;

prop-grid-column-end =
  (
    prop-all |
    nonprop-grid-line
  ) ;

prop-grid-column-start =
  (
    prop-all |
    nonprop-grid-line
  ) ;

prop-grid-row =
  (
    prop-all |
    (
      nonprop-grid-line
      (
        ('/' S)
        nonprop-grid-line
      )?
    )
  ) ;

prop-grid-row-end =
  (
    prop-all |
    nonprop-grid-line
  ) ;

prop-grid-row-start =
  (
    prop-all |
    nonprop-grid-line
  ) ;

prop-grid-template =
  (
    prop-all |
    ('none' S) |
    (
      prop-grid-template-rows
      ('/' S)
      prop-grid-template-columns
    ) |
    (
      (
        nonprop-line-names?
        nonprop-string
        nonprop-track-size?
        nonprop-line-names?
      )+
      (
        ('/' S)
        nonprop-explicit-track-list
      )?
    )
  ) ;

prop-grid-template-areas =
  (
    prop-all |
    ('none' S) |
    nonprop-string+
  ) ;

prop-grid-template-columns =
  (
    prop-all |
    ('none' S) |
    nonprop-track-list |
    nonprop-auto-track-list
  ) ;

prop-grid-template-rows =
  (
    prop-all |
    ('none' S) |
    nonprop-track-list |
    nonprop-auto-track-list
  ) ;

prop-hanging-punctuation =
  (
    prop-all |
    ('none' S) |
    ( (
      ('first' S) |
      (
        ('force-end' S) |
        ('allow-end' S)
      ) |
      ('last' S)
    )+ )
  ) ;

prop-height =
  (
    prop-all |
    (
      (
        nonprop-length |
        nonprop-percentage
      )
      (
        ('border-box' S) |
        ('content-box' S)
      )?
    ) |
    ('available' S) |
    ('min-content' S) |
    ('max-content' S) |
    ('fit-content' S) |
    ('auto' S)
  ) ;

prop-hyphens =
  (
    prop-all |
    ('none' S) |
    ('manual' S) |
    ('auto' S)
  ) ;

prop-image-orientation =
  (
    prop-all |
    ('from-image' S) |
    nonprop-angle |
    (
      nonprop-angle?
      ('flip' S)
    )
  ) ;

prop-image-rendering =
  (
    prop-all |
    ('auto' S) |
    ('crisp-edges' S) |
    ('pixelated' S)
  ) ;

prop-inline-size =
  (
    prop-all |
    prop-width
  ) ;

prop-inset =
  (
    prop-all |
    (
      (
        prop-top |
        prop-top rS
        prop-top |
        prop-top rS
        prop-top rS
        prop-top |
        prop-top rS
        prop-top rS
        prop-top rS
        prop-top
      )
    )
  ) ;

prop-inset-block =
  (
    prop-all |
    (
      (
        prop-top |
        prop-top rS
        prop-top
      )
    )
  ) ;

prop-inset-block-end =
  (
    prop-all |
    prop-top
  ) ;

prop-inset-block-start =
  (
    prop-all |
    prop-top
  ) ;

prop-inset-inline =
  (
    prop-all |
    (
      (
        prop-top |
        prop-top rS
        prop-top
      )
    )
  ) ;

prop-inset-inline-end =
  (
    prop-all |
    prop-top
  ) ;

prop-inset-inline-start =
  (
    prop-all |
    prop-top
  ) ;

prop-isolation =
  (
    prop-all |
    ('auto' S) |
    ('isolate' S)
  ) ;

prop-justify-content =
  (
    prop-all |
    ('normal' S) |
    nonprop-content-distribution |
    (
      nonprop-overflow-position?
      (
        nonprop-content-position |
        ('left' S) |
        ('right' S)
      )
    )
  ) ;

prop-justify-items =
  (
    prop-all |
    ('normal' S) |
    ('stretch' S) |
    nonprop-baseline-position |
    (
      nonprop-overflow-position?
      (
        nonprop-self-position |
        ('left' S) |
        ('right' S)
      )
    ) |
    ('legacy' S) |
    (
      ('legacy' S)
      (
        ('left' S) |
        ('right' S) |
        ('center' S)
      )
    )
  ) ;

prop-justify-self =
  (
    prop-all |
    ('auto' S) |
    ('normal' S) |
    ('stretch' S) |
    nonprop-baseline-position |
    (
      nonprop-overflow-position?
      (
        nonprop-self-position |
        ('left' S) |
        ('right' S)
      )
    )
  ) ;

prop-left =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-letter-spacing =
  (
    prop-all |
    ('normal' S) |
    nonprop-length
  ) ;

prop-line-break =
  (
    prop-all |
    ('auto' S) |
    ('loose' S) |
    ('normal' S) |
    ('strict' S)
  ) ;

prop-line-height =
  (
    prop-all |
    ('normal' S) |
    nonprop-number |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-list-style =
  (
    prop-all |
    ( (
      prop-list-style-type |
      prop-list-style-position |
      prop-list-style-image
    )+ )
  ) ;

prop-list-style-image =
  (
    prop-all |
    nonprop-url |
    ('none' S)
  ) ;

prop-list-style-position =
  (
    prop-all |
    ('inside' S) |
    ('outside' S)
  ) ;

prop-list-style-type =
  (
    prop-all |
    nonprop-counter-style |
    nonprop-string |
    ('none' S)
  ) ;

prop-margin =
  (
    prop-all |
    (
      (
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) |
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) |
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) |
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length |
          nonprop-percentage |
          ('auto' S)
        )
      )
    )
  ) ;

prop-margin-block =
  (
    prop-all |
    (
      (
        prop-margin-left |
        prop-margin-left rS
        prop-margin-left
      )
    )
  ) ;

prop-margin-block-end =
  (
    prop-all |
    prop-margin-left
  ) ;

prop-margin-block-start =
  (
    prop-all |
    prop-margin-left
  ) ;

prop-margin-bottom =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-margin-inline =
  (
    prop-all |
    (
      (
        prop-margin-left |
        prop-margin-left rS
        prop-margin-left
      )
    )
  ) ;

prop-margin-inline-end =
  (
    prop-all |
    prop-margin-left
  ) ;

prop-margin-inline-start =
  (
    prop-all |
    prop-margin-left
  ) ;

prop-margin-left =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-margin-right =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-margin-top =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-mask =
  (
    prop-all |
    nonprop-mask-layer (
      ',' S
      nonprop-mask-layer
    )*
  ) ;

prop-mask-clip =
  (
    prop-all |
    (
      nonprop-geometry-box |
      ('no-clip' S)
    ) (
      ',' S
      (
        nonprop-geometry-box |
        ('no-clip' S)
      )
    )*
  ) ;

prop-mask-composite =
  (
    prop-all |
    nonprop-compositing-operator (
      ',' S
      nonprop-compositing-operator
    )*
  ) ;

prop-mask-image =
  (
    prop-all |
    nonprop-mask-reference (
      ',' S
      nonprop-mask-reference
    )*
  ) ;

prop-mask-mode =
  (
    prop-all |
    nonprop-masking-mode (
      ',' S
      nonprop-masking-mode
    )*
  ) ;

prop-mask-origin =
  (
    prop-all |
    nonprop-geometry-box (
      ',' S
      nonprop-geometry-box
    )*
  ) ;

prop-mask-position =
  (
    prop-all |
    nonprop-position (
      ',' S
      nonprop-position
    )*
  ) ;

prop-mask-repeat =
  (
    prop-all |
    nonprop-repeat-style (
      ',' S
      nonprop-repeat-style
    )*
  ) ;

prop-mask-size =
  (
    prop-all |
    nonprop-bg-size (
      ',' S
      nonprop-bg-size
    )*
  ) ;

prop-mask-type =
  (
    prop-all |
    ('luminance' S) |
    ('alpha' S)
  ) ;

prop-max-height =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('none' S) |
    ('max-content' S) |
    ('min-content' S) |
    ('fit-content' S) |
    ('fill-available' S)
  ) ;

prop-max-width =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('none' S) |
    ('max-content' S) |
    ('min-content' S) |
    ('fit-content' S) |
    ('fill-available' S)
  ) ;

prop-min-block-size =
  (
    prop-all |
    prop-min-width
  ) ;

prop-min-height =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S) |
    ('max-content' S) |
    ('min-content' S) |
    ('fit-content' S) |
    ('fill-available' S)
  ) ;

prop-min-inline-size =
  (
    prop-all |
    prop-min-width
  ) ;

prop-min-width =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S) |
    ('max-content' S) |
    ('min-content' S) |
    ('fit-content' S) |
    ('fill-available' S)
  ) ;

prop-mix-blend-mode =
  (
    prop-all |
    nonprop-blend-mode
  ) ;

prop-object-fit =
  (
    prop-all |
    ('fill' S) |
    ('contain' S) |
    ('cover' S) |
    ('none' S) |
    ('scale-down' S)
  ) ;

prop-object-position =
  (
    prop-all |
    nonprop-position
  ) ;

prop-opacity =
  (
    prop-all |
    nonprop-number
  ) ;

prop-order =
  (
    prop-all |
    nonprop-integer
  ) ;

prop-orphans =
  (
    prop-all |
    nonprop-integer
  ) ;

prop-outline =
  (
    prop-all |
    ( (
      prop-outline-color |
      prop-outline-style |
      prop-outline-width
    )+ )
  ) ;

prop-outline-color =
  (
    prop-all |
    nonprop-color |
    ('invert' S)
  ) ;

prop-outline-offset =
  (
    prop-all |
    nonprop-length
  ) ;

prop-outline-style =
  (
    prop-all |
    ('auto' S) |
    prop-border-style
  ) ;

prop-outline-width =
  (
    prop-all |
    nonprop-line-width
  ) ;

prop-overflow =
  (
    prop-all |
    (
      (
        (
          ('visible' S) |
          ('hidden' S) |
          ('clip' S) |
          ('scroll' S) |
          ('auto' S)
        ) |
        (
          ('visible' S) |
          ('hidden' S) |
          ('clip' S) |
          ('scroll' S) |
          ('auto' S)
        ) rS
        (
          ('visible' S) |
          ('hidden' S) |
          ('clip' S) |
          ('scroll' S) |
          ('auto' S)
        )
      )
    )
  ) ;

prop-overflow-wrap =
  (
    prop-all |
    ('normal' S) |
    ('break-word' S) |
    ('anywhere' S)
  ) ;

prop-overflow-x =
  (
    prop-all |
    ('visible' S) |
    ('hidden' S) |
    ('clip' S) |
    ('scroll' S) |
    ('auto' S)
  ) ;

prop-overflow-y =
  (
    prop-all |
    ('visible' S) |
    ('hidden' S) |
    ('clip' S) |
    ('scroll' S) |
    ('auto' S)
  ) ;

prop-padding =
  (
    prop-all |
    (
      (
        (
          nonprop-length |
          nonprop-percentage
        ) |
        (
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          nonprop-length |
          nonprop-percentage
        ) |
        (
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          nonprop-length |
          nonprop-percentage
        ) |
        (
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          nonprop-length |
          nonprop-percentage
        )
      )
    )
  ) ;

prop-padding-block =
  (
    prop-all |
    (
      (
        prop-padding-left |
        prop-padding-left rS
        prop-padding-left
      )
    )
  ) ;

prop-padding-block-end =
  (
    prop-all |
    prop-padding-left
  ) ;

prop-padding-block-start =
  (
    prop-all |
    prop-padding-left
  ) ;

prop-padding-bottom =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-padding-inline =
  (
    prop-all |
    (
      (
        prop-padding-left |
        prop-padding-left rS
        prop-padding-left
      )
    )
  ) ;

prop-padding-inline-end =
  (
    prop-all |
    prop-padding-left
  ) ;

prop-padding-inline-start =
  (
    prop-all |
    prop-padding-left
  ) ;

prop-padding-left =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-padding-right =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-padding-top =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-page-break-after =
  (
    prop-all |
    ('auto' S) |
    ('always' S) |
    ('avoid' S) |
    ('left' S) |
    ('right' S) |
    ('recto' S) |
    ('verso' S)
  ) ;

prop-page-break-before =
  (
    prop-all |
    ('auto' S) |
    ('always' S) |
    ('avoid' S) |
    ('left' S) |
    ('right' S) |
    ('recto' S) |
    ('verso' S)
  ) ;

prop-page-break-inside =
  (
    prop-all |
    ('auto' S) |
    ('avoid' S)
  ) ;

prop-perspective =
  (
    prop-all |
    ('none' S) |
    nonprop-length
  ) ;

prop-perspective-origin =
  (
    prop-all |
    nonprop-position
  ) ;

prop-place-content =
  (
    prop-all |
    (
      prop-align-content
      prop-justify-content?
    )
  ) ;

prop-place-items =
  (
    prop-all |
    (
      prop-align-items
      prop-justify-items?
    )
  ) ;

prop-place-self =
  (
    prop-all |
    (
      prop-align-self
      prop-justify-self?
    )
  ) ;

prop-pointer-events =
  (
    prop-all |
    ('auto' S) |
    ('none' S) |
    ('visiblePainted' S) |
    ('visibleFill' S) |
    ('visibleStroke' S) |
    ('visible' S) |
    ('painted' S) |
    ('fill' S) |
    ('stroke' S) |
    ('all' S) |
    ('inherit' S)
  ) ;

prop-position =
  (
    prop-all |
    ('static' S) |
    ('relative' S) |
    ('absolute' S) |
    ('sticky' S) |
    ('fixed' S)
  ) ;

prop-quotes =
  (
    prop-all |
    ('none' S) |
    (
      nonprop-string
      nonprop-string
    )+
  ) ;

prop-resize =
  (
    prop-all |
    ('none' S) |
    ('both' S) |
    ('horizontal' S) |
    ('vertical' S) |
    ('block' S) |
    ('inline' S)
  ) ;

prop-right =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-rotate =
  (
    prop-all |
    ('none' S) |
    nonprop-angle |
    (
      (
        ('x' S) |
        ('y' S) |
        ('z' S) |
        (
          (
            nonprop-number rS
            nonprop-number rS
            nonprop-number
          )
        )
      )
      nonprop-angle
    )
  ) ;

prop-row-gap =
  (
    prop-all |
    ('normal' S) |
    nonprop-length-percentage
  ) ;

prop-scale =
  (
    prop-all |
    ('none' S) |
    (
      (
        nonprop-number |
        nonprop-number rS
        nonprop-number |
        nonprop-number rS
        nonprop-number rS
        nonprop-number
      )
    )
  ) ;

prop-scroll-behavior =
  (
    prop-all |
    ('auto' S) |
    ('smooth' S)
  ) ;

prop-scroll-margin =
  (
    prop-all |
    (
      (
        (
          ('auto' S) |
          nonprop-length
        ) |
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        ) |
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        ) |
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        )
      )
    )
  ) ;

prop-scroll-margin-block =
  (
    prop-all |
    (
      (
        (
          ('auto' S) |
          nonprop-length
        ) |
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        )
      )
    )
  ) ;

prop-scroll-margin-block-end =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-block-start =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-bottom =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-inline =
  (
    prop-all |
    (
      (
        (
          ('auto' S) |
          nonprop-length
        ) |
        (
          ('auto' S) |
          nonprop-length
        ) rS
        (
          ('auto' S) |
          nonprop-length
        )
      )
    )
  ) ;

prop-scroll-margin-inline-end =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-inline-start =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-left =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-right =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-margin-top =
  (
    prop-all |
    ('auto' S) |
    nonprop-length
  ) ;

prop-scroll-padding =
  (
    prop-all |
    (
      (
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) |
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) |
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) |
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        )
      )
    )
  ) ;

prop-scroll-padding-block =
  (
    prop-all |
    (
      (
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) |
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        )
      )
    )
  ) ;

prop-scroll-padding-block-end =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-block-start =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-bottom =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-inline =
  (
    prop-all |
    (
      (
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) |
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        ) rS
        (
          ('auto' S) |
          nonprop-length |
          nonprop-percentage
        )
      )
    )
  ) ;

prop-scroll-padding-inline-end =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-inline-start =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-left =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-right =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-padding-top =
  (
    prop-all |
    ('auto' S) |
    nonprop-length |
    nonprop-percentage
  ) ;

prop-scroll-snap-align =
  (
    prop-all |
    (
      (
        (
          ('none' S) |
          ('start' S) |
          ('end' S) |
          ('center' S)
        ) |
        (
          ('none' S) |
          ('start' S) |
          ('end' S) |
          ('center' S)
        ) rS
        (
          ('none' S) |
          ('start' S) |
          ('end' S) |
          ('center' S)
        )
      )
    )
  ) ;

prop-scroll-snap-stop =
  (
    prop-all |
    ('normal' S) |
    ('always' S)
  ) ;

prop-scroll-snap-type =
  (
    prop-all |
    ('none' S) |
    (
      (
        ('x' S) |
        ('y' S) |
        ('block' S) |
        ('inline' S) |
        ('both' S)
      )
      (
        ('mandatory' S) |
        ('proximity' S)
      )
    )
  ) ;

prop-scrollbar-color =
  (
    prop-all |
    ('auto' S) |
    ('dark' S) |
    ('light' S) |
    (
      (
        nonprop-color rS
        nonprop-color
      )
    )
  ) ;

prop-scrollbar-width =
  (
    prop-all |
    ('auto' S) |
    ('thin' S) |
    ('none' S)
  ) ;

prop-shape-image-threshold =
  (
    prop-all |
    nonprop-number
  ) ;

prop-shape-margin =
  (
    prop-all |
    nonprop-length-percentage
  ) ;

prop-shape-outside =
  (
    prop-all |
    ('none' S) |
    ( (
      nonprop-shape-box |
      nonprop-basic-shape
    )+ ) |
    nonprop-image
  ) ;

prop-tab-size =
  (
    prop-all |
    nonprop-integer |
    nonprop-length
  ) ;

prop-table-layout =
  (
    prop-all |
    ('auto' S) |
    ('fixed' S)
  ) ;

prop-text-align =
  (
    prop-all |
    ('start' S) |
    ('end' S) |
    ('left' S) |
    ('right' S) |
    ('center' S) |
    ('justify' S) |
    ('match-parent' S)
  ) ;

prop-text-align-last =
  (
    prop-all |
    ('auto' S) |
    ('start' S) |
    ('end' S) |
    ('left' S) |
    ('right' S) |
    ('center' S) |
    ('justify' S)
  ) ;

prop-text-combine-upright =
  (
    prop-all |
    ('none' S) |
    ('all' S) |
    (
      ('digits' S)
      nonprop-integer?
    )
  ) ;

prop-text-decoration =
  (
    prop-all |
    ( (
      prop-text-decoration-line |
      prop-text-decoration-style |
      prop-text-decoration-color
    )+ )
  ) ;

prop-text-decoration-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-text-decoration-line =
  (
    prop-all |
    ('none' S) |
    ( (
      ('underline' S) |
      ('overline' S) |
      ('line-through' S) |
      ('blink' S)
    )+ )
  ) ;

prop-text-decoration-style =
  (
    prop-all |
    ('solid' S) |
    ('double' S) |
    ('dotted' S) |
    ('dashed' S) |
    ('wavy' S)
  ) ;

prop-text-emphasis =
  (
    prop-all |
    ( (
      prop-text-emphasis-style |
      prop-text-emphasis-color
    )+ )
  ) ;

prop-text-emphasis-color =
  (
    prop-all |
    nonprop-color
  ) ;

prop-text-emphasis-position =
  (
    prop-all |
    (
      (
        ('over' S) |
        ('under' S)
      )
      (
        ('right' S) |
        ('left' S)
      )
    )
  ) ;

prop-text-emphasis-style =
  (
    prop-all |
    ('none' S) |
    ( (
      (
        ('filled' S) |
        ('open' S)
      ) |
      (
        ('dot' S) |
        ('circle' S) |
        ('double-circle' S) |
        ('triangle' S) |
        ('sesame' S)
      )
    )+ ) |
    nonprop-string
  ) ;

prop-text-indent =
  (
    prop-all |
    (
      nonprop-length-percentage
      ('hanging' S)?
      ('each-line' S)?
    )
  ) ;

prop-text-justify =
  (
    prop-all |
    ('auto' S) |
    ('inter-character' S) |
    ('inter-word' S) |
    ('none' S)
  ) ;

prop-text-orientation =
  (
    prop-all |
    ('mixed' S) |
    ('upright' S) |
    ('sideways' S)
  ) ;

prop-text-overflow =
  (
    prop-all |
    (
      (
        (
          ('clip' S) |
          ('ellipsis' S) |
          nonprop-string
        ) |
        (
          ('clip' S) |
          ('ellipsis' S) |
          nonprop-string
        ) rS
        (
          ('clip' S) |
          ('ellipsis' S) |
          nonprop-string
        )
      )
    )
  ) ;

prop-text-rendering =
  (
    prop-all |
    ('auto' S) |
    ('optimizeSpeed' S) |
    ('optimizeLegibility' S) |
    ('geometricPrecision' S)
  ) ;

prop-text-shadow =
  (
    prop-all |
    ('none' S) |
    nonprop-shadow-t (
      ',' S
      nonprop-shadow-t
    )*
  ) ;

prop-text-transform =
  (
    prop-all |
    ('none' S) |
    ('capitalize' S) |
    ('uppercase' S) |
    ('lowercase' S) |
    ('full-width' S) |
    ('full-size-kana' S)
  ) ;

prop-text-underline-position =
  (
    prop-all |
    ('auto' S) |
    ( (
      ('under' S) |
      (
        ('left' S) |
        ('right' S)
      )
    )+ )
  ) ;

prop-top =
  (
    prop-all |
    nonprop-length |
    nonprop-percentage |
    ('auto' S)
  ) ;

prop-touch-action =
  (
    prop-all |
    ('auto' S) |
    ('none' S) |
    ( (
      (
        ('pan-x' S) |
        ('pan-left' S) |
        ('pan-right' S)
      ) |
      (
        ('pan-y' S) |
        ('pan-up' S) |
        ('pan-down' S)
      ) |
      ('pinch-zoom' S)
    )+ ) |
    ('manipulation' S)
  ) ;

prop-transform =
  (
    prop-all |
    ('none' S) |
    nonprop-transform-list
  ) ;

prop-transform-box =
  (
    prop-all |
    ('border-box' S) |
    ('fill-box' S) |
    ('view-box' S)
  ) ;

prop-transform-origin =
  (
    prop-all |
    (
      nonprop-length-percentage |
      ('left' S) |
      ('center' S) |
      ('right' S) |
      ('top' S) |
      ('bottom' S)
    ) |
    (
      (
        (
          nonprop-length-percentage |
          ('left' S) |
          ('center' S) |
          ('right' S)
        )
        (
          nonprop-length-percentage |
          ('top' S) |
          ('center' S) |
          ('bottom' S)
        )
      )
      nonprop-length?
    )
  ) ;

prop-transform-style =
  (
    prop-all |
    ('flat' S) |
    ('preserve-3d' S)
  ) ;

prop-transition =
  (
    prop-all |
    nonprop-single-transition (
      ',' S
      nonprop-single-transition
    )*
  ) ;

prop-transition-delay =
  (
    prop-all |
    nonprop-time (
      ',' S
      nonprop-time
    )*
  ) ;

prop-transition-duration =
  (
    prop-all |
    nonprop-time (
      ',' S
      nonprop-time
    )*
  ) ;

prop-transition-property =
  (
    prop-all |
    ('none' S) |
    nonprop-single-transition-property (
      ',' S
      nonprop-single-transition-property
    )*
  ) ;

prop-transition-timing-function =
  (
    prop-all |
    nonprop-timing-function (
      ',' S
      nonprop-timing-function
    )*
  ) ;

prop-translate =
  (
    prop-all |
    ('none' S) |
    (
      nonprop-length-percentage
      (
        nonprop-length-percentage
        nonprop-length?
      )?
    )
  ) ;

prop-unicode-bidi =
  (
    prop-all |
    ('normal' S) |
    ('embed' S) |
    ('isolate' S) |
    ('bidi-override' S) |
    ('isolate-override' S) |
    ('plaintext' S)
  ) ;

prop-vertical-align =
  (
    prop-all |
    ('baseline' S) |
    ('sub' S) |
    ('super' S) |
    ('text-top' S) |
    ('text-bottom' S) |
    ('middle' S) |
    ('top' S) |
    ('bottom' S) |
    nonprop-percentage |
    nonprop-length
  ) ;

prop-visibility =
  (
    prop-all |
    ('visible' S) |
    ('hidden' S) |
    ('collapse' S)
  ) ;

prop-white-space =
  (
    prop-all |
    ('normal' S) |
    ('pre' S) |
    ('nowrap' S) |
    ('pre-wrap' S) |
    ('pre-line' S)
  ) ;

prop-widows =
  (
    prop-all |
    nonprop-integer
  ) ;

prop-width =
  (
    prop-all |
    (
      (
        nonprop-length |
        nonprop-percentage
      )
      (
        ('border-box' S) |
        ('content-box' S)
      )?
    ) |
    ('available' S) |
    ('min-content' S) |
    ('max-content' S) |
    ('fit-content' S) |
    ('auto' S)
  ) ;

prop-will-change =
  (
    prop-all |
    ('auto' S) |
    nonprop-animateable-feature (
      ',' S
      nonprop-animateable-feature
    )*
  ) ;

prop-word-break =
  (
    prop-all |
    ('normal' S) |
    ('break-all' S) |
    ('keep-all' S) |
    ('break-word' S)
  ) ;

prop-word-spacing =
  (
    prop-all |
    ('normal' S) |
    nonprop-length-percentage
  ) ;

prop-word-wrap =
  (
    prop-all |
    ('normal' S) |
    ('break-word' S)
  ) ;

prop-writing-mode =
  (
    prop-all |
    ('horizontal-tb' S) |
    ('vertical-rl' S) |
    ('vertical-lr' S) |
    ('sideways-rl' S) |
    ('sideways-lr' S)
  ) ;

prop-z-index =
  (
    prop-all |
    ('auto' S) |
    nonprop-integer
  ) ;

nonprop-absolute-size =
  (
    ('xx-small' S) |
    ('x-small' S) |
    ('small' S) |
    ('medium' S) |
    ('large' S) |
    ('x-large' S) |
    ('xx-large' S)
  ) ;

nonprop-alpha-value =
  (
    nonprop-number |
    nonprop-percentage
  ) ;

nonprop-angle-percentage =
  (
    nonprop-angle |
    nonprop-percentage
  ) ;

nonprop-angular-color-hint =
  nonprop-angle-percentage ;

nonprop-angular-color-stop =
  (
    nonprop-color
    nonprop-color-stop-angle?
  ) ;

nonprop-angular-color-stop-list =
  (
    (
      nonprop-angular-color-stop
      (
        (',' S)
        nonprop-angular-color-hint
      )?
    ) (
      ',' S
      (
        nonprop-angular-color-stop
        (
          (',' S)
          nonprop-angular-color-hint
        )?
      )
    )*
    (',' S)
    nonprop-angular-color-stop
  ) ;

nonprop-animateable-feature =
  (
    ('scroll-position' S) |
    ('contents' S) |
    nonprop-custom-ident
  ) ;

nonprop-attachment =
  (
    ('scroll' S) |
    ('fixed' S) |
    ('local' S)
  ) ;

func-attr =
  (
    ('attr' S)
    ('(' S)
    nonprop-attr-name
    nonprop-type-or-unit?
    (
      (',' S)
      nonprop-attr-fallback
    )?
    (')' S)
  ) ;

nonprop-attr-matcher =
  (
    (
      ('~' S) |
      ('|' S) |
      ('^' S) |
      ('$' S) |
      ('*' S)
    )?
    ('=' S)
  ) ;

nonprop-attr-modifier =
  (
    ('i' S) |
    ('s' S)
  ) ;

nonprop-attribute-selector =
  (
    (
      ('[' S)
      nonprop-wq-name
      (']' S)
    ) |
    (
      ('[' S)
      nonprop-wq-name
      nonprop-attr-matcher
      (
        nonprop-string-token |
        nonprop-ident-token
      )
      nonprop-attr-modifier?
      (']' S)
    )
  ) ;

nonprop-auto-repeat =
  (
    ('repeat' S)
    ('(' S)
    (
      ('auto-fill' S) |
      ('auto-fit' S)
    )
    (',' S)
    (
      nonprop-line-names?
      nonprop-fixed-size
    )+
    nonprop-line-names?
    (')' S)
  ) ;

nonprop-auto-track-list =
  (
    (
      nonprop-line-names?
      (
        nonprop-fixed-size |
        nonprop-fixed-repeat
      )
    )*
    nonprop-line-names?
    nonprop-auto-repeat
    (
      nonprop-line-names?
      (
        nonprop-fixed-size |
        nonprop-fixed-repeat
      )
    )*
    nonprop-line-names?
  ) ;

nonprop-baseline-position =
  (
    (
      ('first' S) |
      ('last' S)
    )?
    ('baseline' S)
  ) ;

nonprop-basic-shape =
  (
    func-inset |
    func-circle |
    func-ellipse |
    func-polygon
  ) ;

nonprop-bg-image =
  (
    ('none' S) |
    nonprop-image
  ) ;

nonprop-bg-layer =
  ( (
    nonprop-bg-image |
    (
      nonprop-bg-position
      (
        ('/' S)
        nonprop-bg-size
      )?
    ) |
    nonprop-repeat-style |
    nonprop-attachment |
    nonprop-box |
    nonprop-box
  )+ ) ;

nonprop-bg-position =
  (
    (
      ('left' S) |
      ('center' S) |
      ('right' S) |
      ('top' S) |
      ('bottom' S) |
      nonprop-length-percentage
    ) |
    (
      (
        ('left' S) |
        ('center' S) |
        ('right' S) |
        nonprop-length-percentage
      )
      (
        ('top' S) |
        ('center' S) |
        ('bottom' S) |
        nonprop-length-percentage
      )
    ) |
    (
      (
        ('center' S) |
        (
          (
            ('left' S) |
            ('right' S)
          )
          nonprop-length-percentage?
        )
      )
      (
        ('center' S) |
        (
          (
            ('top' S) |
            ('bottom' S)
          )
          nonprop-length-percentage?
        )
      )
    )
  ) ;

nonprop-bg-size =
  (
    (
      (
        (
          nonprop-length-percentage |
          ('auto' S)
        ) |
        (
          nonprop-length-percentage |
          ('auto' S)
        ) rS
        (
          nonprop-length-percentage |
          ('auto' S)
        )
      )
    ) |
    ('cover' S) |
    ('contain' S)
  ) ;

nonprop-blend-mode =
  (
    ('normal' S) |
    ('multiply' S) |
    ('screen' S) |
    ('overlay' S) |
    ('darken' S) |
    ('lighten' S) |
    ('color-dodge' S) |
    ('color-burn' S) |
    ('hard-light' S) |
    ('soft-light' S) |
    ('difference' S) |
    ('exclusion' S) |
    ('hue' S) |
    ('saturation' S) |
    ('color' S) |
    ('luminosity' S)
  ) ;

func-blur =
  (
    ('blur' S)
    ('(' S)
    nonprop-length
    (')' S)
  ) ;

nonprop-box =
  (
    ('border-box' S) |
    ('padding-box' S) |
    ('content-box' S)
  ) ;

func-brightness =
  (
    ('brightness' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

func-calc =
  (
    ('calc' S)
    ('(' S)
    nonprop-calc-sum
    (')' S)
  ) ;

nonprop-calc-product =
  (
    nonprop-calc-value
    (
      (
        ('*' S)
        nonprop-calc-value
      ) |
      (
        ('/' S)
        nonprop-number
      )
    )*
  ) ;

nonprop-calc-sum =
  (
    nonprop-calc-product
    (
      (
        ('+' S) |
        ('-' S)
      )
      nonprop-calc-product
    )*
  ) ;

nonprop-calc-value =
  (
    nonprop-number |
    nonprop-dimension |
    nonprop-percentage |
    (
      ('(' S)
      nonprop-calc-sum
      (')' S)
    )
  ) ;

nonprop-cf-final-image =
  (
    nonprop-image |
    nonprop-color
  ) ;

nonprop-cf-mixing-image =
  (
    nonprop-percentage?
    nonprop-image
  ) ;

func-circle =
  (
    ('circle' S)
    ('(' S)
    nonprop-shape-radius?
    (
      ('at' S)
      nonprop-position
    )?
    (')' S)
  ) ;

func-clamp =
  (
    ('clamp' S)
    ('(' S)
    (
      (
        nonprop-calc-sum ',' S
        nonprop-calc-sum ',' S
        nonprop-calc-sum
      )
    )
    (')' S)
  ) ;

nonprop-class-selector =
  (
    ('.' S)
    nonprop-ident-token
  ) ;

nonprop-clip-source =
  nonprop-url ;

nonprop-color =
  (
    func-rgb |
    func-rgba |
    func-hsl |
    func-hsla |
    nonprop-hex-color |
    nonprop-named-color |
    ('currentcolor' S) |
    nonprop-deprecated-system-color
  ) ;

nonprop-color-stop =
  (
    nonprop-color-stop-length |
    nonprop-color-stop-angle
  ) ;

nonprop-color-stop-angle =
  (
    (
      nonprop-angle-percentage |
      nonprop-angle-percentage rS
      nonprop-angle-percentage
    )
  ) ;

nonprop-color-stop-length =
  (
    (
      nonprop-length-percentage |
      nonprop-length-percentage rS
      nonprop-length-percentage
    )
  ) ;

nonprop-color-stop-list =
  (
    (
      nonprop-linear-color-stop
      (
        (',' S)
        nonprop-linear-color-hint
      )?
    ) (
      ',' S
      (
        nonprop-linear-color-stop
        (
          (',' S)
          nonprop-linear-color-hint
        )?
      )
    )*
    (',' S)
    nonprop-linear-color-stop
  ) ;

nonprop-combinator =
  (
    ('>' S) |
    ('+' S) |
    ('~' S) |
    ('||' S)
  ) ;

nonprop-common-lig-values =
  (
    ('common-ligatures' S) |
    ('no-common-ligatures' S)
  ) ;

nonprop-compat =
  (
    ('searchfield' S) |
    ('textarea' S) |
    ('push-button' S) |
    ('button-bevel' S) |
    ('slider-horizontal' S) |
    ('checkbox' S) |
    ('radio' S) |
    ('square-button' S) |
    ('menulist' S) |
    ('menulist-button' S) |
    ('listbox' S) |
    ('meter' S) |
    ('progress-bar' S)
  ) ;

nonprop-complex-selector =
  (
    nonprop-compound-selector
    (
      nonprop-combinator?
      nonprop-compound-selector
    )*
  ) ;

nonprop-complex-selector-list =
  nonprop-complex-selector (
    ',' S
    nonprop-complex-selector
  )* ;

nonprop-composite-style =
  (
    ('clear' S) |
    ('copy' S) |
    ('source-over' S) |
    ('source-in' S) |
    ('source-out' S) |
    ('source-atop' S) |
    ('destination-over' S) |
    ('destination-in' S) |
    ('destination-out' S) |
    ('destination-atop' S) |
    ('xor' S)
  ) ;

nonprop-compositing-operator =
  (
    ('add' S) |
    ('subtract' S) |
    ('intersect' S) |
    ('exclude' S)
  ) ;

nonprop-compound-selector =
  (
    nonprop-type-selector?
    nonprop-subclass-selector*
    (
      nonprop-pseudo-element-selector
      nonprop-pseudo-class-selector*
    )*
  ) ;

nonprop-compound-selector-list =
  nonprop-compound-selector (
    ',' S
    nonprop-compound-selector
  )* ;

func-conic-gradient =
  (
    ('conic-gradient' S)
    ('(' S)
    (
      ('from' S)
      nonprop-angle
    )?
    (
      ('at' S)
      nonprop-position
    )?
    ',' S
    nonprop-angular-color-stop-list
    (')' S)
  ) ;

nonprop-content-distribution =
  (
    ('space-between' S) |
    ('space-around' S) |
    ('space-evenly' S) |
    ('stretch' S)
  ) ;

nonprop-content-list =
  (
    nonprop-string |
    ('contents' S) |
    nonprop-image |
    nonprop-quote |
    nonprop-target |
    func-leader
  )+ ;

nonprop-content-position =
  (
    ('center' S) |
    ('start' S) |
    ('end' S) |
    ('flex-start' S) |
    ('flex-end' S)
  ) ;

nonprop-content-replacement =
  nonprop-image ;

nonprop-contextual-alt-values =
  (
    ('contextual' S) |
    ('no-contextual' S)
  ) ;

func-contrast =
  (
    ('contrast' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

func-counter =
  (
    ('counter' S)
    ('(' S)
    nonprop-custom-ident
    (',' S)
    (
      nonprop-counter-style |
      ('none' S)
    )?
    (')' S)
  ) ;

nonprop-counter-style =
  (
    nonprop-counter-style-name |
    (
      ('symbols' S)
      ('(' S)
      (')' S)
    )
  ) ;

nonprop-counter-style-name =
  nonprop-custom-ident ;

func-counters =
  (
    ('counters' S)
    ('(' S)
    nonprop-custom-ident
    (',' S)
    nonprop-string
    ',' S
    (
      nonprop-counter-style |
      ('none' S)
    )?
    (')' S)
  ) ;

func-cross-fade =
  (
    ('cross-fade' S)
    ('(' S)
    nonprop-cf-mixing-image
    (',' S)
    nonprop-cf-final-image?
    (')' S)
  ) ;

nonprop-cubic-bezier-timing-function =
  (
    ('ease' S) |
    ('ease-in' S) |
    ('ease-out' S) |
    ('ease-in-out' S) |
    (
      ('cubic-bezier' S)
      ('(' S)
      nonprop-number
      ',' S
      nonprop-number
      (',' S)
      nonprop-number
      ',' S
      nonprop-number
      (')' S)
    )
  ) ;

nonprop-deprecated-system-color =
  (
    ('ActiveBorder' S) |
    ('ActiveCaption' S) |
    ('AppWorkspace' S) |
    ('Background' S) |
    ('ButtonFace' S) |
    ('ButtonHighlight' S) |
    ('ButtonShadow' S) |
    ('ButtonText' S) |
    ('CaptionText' S) |
    ('GrayText' S) |
    ('Highlight' S) |
    ('HighlightText' S) |
    ('InactiveBorder' S) |
    ('InactiveCaption' S) |
    ('InactiveCaptionText' S) |
    ('InfoBackground' S) |
    ('InfoText' S) |
    ('Menu' S) |
    ('MenuText' S) |
    ('Scrollbar' S) |
    ('ThreeDDarkShadow' S) |
    ('ThreeDFace' S) |
    ('ThreeDHighlight' S) |
    ('ThreeDLightShadow' S) |
    ('ThreeDShadow' S) |
    ('Window' S) |
    ('WindowFrame' S) |
    ('WindowText' S)
  ) ;

nonprop-discretionary-lig-values =
  (
    ('discretionary-ligatures' S) |
    ('no-discretionary-ligatures' S)
  ) ;

nonprop-display-box =
  (
    ('contents' S) |
    ('none' S)
  ) ;

nonprop-display-inside =
  (
    ('flow' S) |
    ('flow-root' S) |
    ('table' S) |
    ('flex' S) |
    ('grid' S) |
    ('ruby' S)
  ) ;

nonprop-display-internal =
  (
    ('table-row-group' S) |
    ('table-header-group' S) |
    ('table-footer-group' S) |
    ('table-row' S) |
    ('table-cell' S) |
    ('table-column-group' S) |
    ('table-column' S) |
    ('table-caption' S) |
    ('ruby-base' S) |
    ('ruby-text' S) |
    ('ruby-base-container' S) |
    ('ruby-text-container' S)
  ) ;

nonprop-display-legacy =
  (
    ('inline-block' S) |
    ('inline-list-item' S) |
    ('inline-table' S) |
    ('inline-flex' S) |
    ('inline-grid' S)
  ) ;

nonprop-display-listitem =
  (
    nonprop-display-outside?
    (
      ('flow' S) |
      ('flow-root' S)
    )?
    ('list-item' S)
  ) ;

nonprop-display-outside =
  (
    ('block' S) |
    ('inline' S) |
    ('run-in' S)
  ) ;

func-drop-shadow =
  (
    ('drop-shadow' S)
    ('(' S)
    (
      (
        nonprop-length rS
        nonprop-length |
        nonprop-length rS
        nonprop-length rS
        nonprop-length
      )
    )
    nonprop-color?
    (')' S)
  ) ;

nonprop-east-asian-variant-values =
  (
    ('jis78' S) |
    ('jis83' S) |
    ('jis90' S) |
    ('jis04' S) |
    ('simplified' S) |
    ('traditional' S)
  ) ;

nonprop-east-asian-width-values =
  (
    ('full-width' S) |
    ('proportional-width' S)
  ) ;

func-element =
  (
    ('element' S)
    ('(' S)
    nonprop-id-selector
    (')' S)
  ) ;

func-ellipse =
  (
    ('ellipse' S)
    ('(' S)
    (
      (
        nonprop-shape-radius rS
        nonprop-shape-radius
      )
    )?
    (
      ('at' S)
      nonprop-position
    )?
    (')' S)
  ) ;

nonprop-ending-shape =
  (
    ('circle' S) |
    ('ellipse' S)
  ) ;

func-env =
  (
    ('env' S)
    ('(' S)
    nonprop-custom-ident
    (',' S)
    nonprop-declaration-value?
    (')' S)
  ) ;

nonprop-explicit-track-list =
  (
    (
      nonprop-line-names?
      nonprop-track-size
    )+
    nonprop-line-names?
  ) ;

nonprop-family-name =
  (
    nonprop-string |
    nonprop-custom-ident+
  ) ;

nonprop-feature-tag-value =
  (
    nonprop-string
    (
      nonprop-integer |
      ('on' S) |
      ('off' S)
    )?
  ) ;

nonprop-feature-type =
  (
    ('@stylistic' S) |
    ('@historical-forms' S) |
    ('@styleset' S) |
    ('@character-variant' S) |
    ('@swash' S) |
    ('@ornaments' S) |
    ('@annotation' S)
  ) ;

nonprop-feature-value-block =
  (
    nonprop-feature-type
    ('{' S)
    nonprop-feature-value-declaration-list
    ('}' S)
  ) ;

nonprop-feature-value-block-list =
  nonprop-feature-value-block+ ;

nonprop-feature-value-declaration =
  (
    nonprop-custom-ident
    (':' S)
    nonprop-integer+
    (';' S)
  ) ;

nonprop-feature-value-declaration-list =
  nonprop-feature-value-declaration ;

nonprop-feature-value-name =
  nonprop-custom-ident ;

nonprop-fill-rule =
  (
    ('nonzero' S) |
    ('evenodd' S)
  ) ;

nonprop-filter-function =
  (
    func-blur |
    func-brightness |
    func-contrast |
    func-drop-shadow |
    func-grayscale |
    func-hue-rotate |
    func-invert |
    func-opacity |
    func-saturate |
    func-sepia
  ) ;

nonprop-filter-function-list =
  (
    nonprop-filter-function |
    nonprop-url
  )+ ;

nonprop-final-bg-layer =
  ( (
    prop-background-color |
    nonprop-bg-image |
    (
      nonprop-bg-position
      (
        ('/' S)
        nonprop-bg-size
      )?
    ) |
    nonprop-repeat-style |
    nonprop-attachment |
    nonprop-box |
    nonprop-box
  )+ ) ;

func-fit-content =
  (
    ('fit-content' S)
    ('(' S)
    (
      nonprop-length |
      nonprop-percentage
    )
    (')' S)
  ) ;

nonprop-fixed-breadth =
  nonprop-length-percentage ;

nonprop-fixed-repeat =
  (
    ('repeat' S)
    ('(' S)
    nonprop-positive-integer
    (',' S)
    (
      nonprop-line-names?
      nonprop-fixed-size
    )+
    nonprop-line-names?
    (')' S)
  ) ;

nonprop-fixed-size =
  (
    nonprop-fixed-breadth |
    (
      ('minmax' S)
      ('(' S)
      nonprop-fixed-breadth
      (',' S)
      nonprop-track-breadth
      (')' S)
    ) |
    (
      ('minmax' S)
      ('(' S)
      nonprop-inflexible-breadth
      (',' S)
      nonprop-fixed-breadth
      (')' S)
    )
  ) ;

nonprop-font-display =
  (
    ('auto' S) |
    ('block' S) |
    ('swap' S) |
    ('fallback' S) |
    ('optional' S)
  ) ;

nonprop-font-stretch-absolute =
  (
    ('normal' S) |
    ('ultra-condensed' S) |
    ('extra-condensed' S) |
    ('condensed' S) |
    ('semi-condensed' S) |
    ('semi-expanded' S) |
    ('expanded' S) |
    ('extra-expanded' S) |
    ('ultra-expanded' S) |
    nonprop-percentage
  ) ;

nonprop-font-variant-css21 =
  (
    ('normal' S) |
    ('small-caps' S)
  ) ;

nonprop-font-weight-absolute =
  (
    ('normal' S) |
    ('bold' S) |
    nonprop-number
  ) ;

nonprop-frequency-percentage =
  (
    nonprop-frequency |
    nonprop-percentage
  ) ;

nonprop-general-enclosed =
  (
    (
      nonprop-function-token
      nonprop-any-value
      (')' S)
    ) |
    (
      ('(' S)
      nonprop-ident
      nonprop-any-value
      (')' S)
    )
  ) ;

nonprop-generic-family =
  (
    ('serif' S) |
    ('sans-serif' S) |
    ('cursive' S) |
    ('fantasy' S) |
    ('monospace' S)
  ) ;

nonprop-generic-name =
  (
    ('serif' S) |
    ('sans-serif' S) |
    ('cursive' S) |
    ('fantasy' S) |
    ('monospace' S)
  ) ;

nonprop-geometry-box =
  (
    nonprop-shape-box |
    ('fill-box' S) |
    ('stroke-box' S) |
    ('view-box' S)
  ) ;

nonprop-gradient =
  (
    func-linear-gradient |
    func-repeating-linear-gradient |
    func-radial-gradient |
    func-repeating-radial-gradient |
    func-conic-gradient
  ) ;

func-grayscale =
  (
    ('grayscale' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

nonprop-grid-line =
  (
    ('auto' S) |
    nonprop-custom-ident |
    (
      nonprop-integer
      nonprop-custom-ident?
    ) |
    (
      ('span' S)
      ( (
        nonprop-integer |
        nonprop-custom-ident
      )+ )
    )
  ) ;

nonprop-historical-lig-values =
  (
    ('historical-ligatures' S) |
    ('no-historical-ligatures' S)
  ) ;

func-hsl =
  (
    (
      ('hsl' S)
      ('(' S)
      nonprop-hue
      nonprop-percentage
      nonprop-percentage
      (
        ('/' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('hsl' S)
      ('(' S)
      nonprop-hue
      (',' S)
      nonprop-percentage
      (',' S)
      nonprop-percentage
      (',' S)
      nonprop-alpha-value?
      (')' S)
    )
  ) ;

func-hsla =
  (
    (
      ('hsla' S)
      ('(' S)
      nonprop-hue
      nonprop-percentage
      nonprop-percentage
      (
        ('/' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('hsla' S)
      ('(' S)
      nonprop-hue
      (',' S)
      nonprop-percentage
      (',' S)
      nonprop-percentage
      ',' S
      nonprop-alpha-value?
      (')' S)
    )
  ) ;

nonprop-hue =
  (
    nonprop-number |
    nonprop-angle
  ) ;

func-hue-rotate =
  (
    ('hue-rotate' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

nonprop-id-selector =
  nonprop-hash-token ;

nonprop-image =
  (
    nonprop-url |
    func-image |
    func-image-set |
    func-element |
    func-cross-fade |
    nonprop-gradient
  ) ;

func-image =
  (
    ('image' S)
    ('(' S)
    nonprop-image-tags?
    (
      nonprop-image-src?
      (',' S)
      nonprop-color?
    )
    (')' S)
  ) ;

func-image-set =
  (
    ('image-set' S)
    ('(' S)
    nonprop-image-set-option (
      ',' S
      nonprop-image-set-option
    )*
    (')' S)
  ) ;

nonprop-image-set-option =
  (
    (
      nonprop-image |
      nonprop-string
    )
    nonprop-resolution
  ) ;

nonprop-image-src =
  (
    nonprop-url |
    nonprop-string
  ) ;

nonprop-image-tags =
  (
    ('ltr' S) |
    ('rtl' S)
  ) ;

nonprop-inflexible-breadth =
  (
    nonprop-length |
    nonprop-percentage |
    ('min-content' S) |
    ('max-content' S) |
    ('auto' S)
  ) ;

func-inset =
  (
    ('inset' S)
    ('(' S)
    (
      (
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage rS
        nonprop-length-percentage |
        nonprop-length-percentage rS
        nonprop-length-percentage rS
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
    (
      ('round' S)
      prop-border-radius
    )?
    (')' S)
  ) ;

func-invert =
  (
    ('invert' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

nonprop-keyframe-block =
  (
    nonprop-keyframe-selector (
      ',' S
      nonprop-keyframe-selector
    )*
    '{' S
      nonprop-declaration-list
    '}' S
  ) ;

nonprop-keyframe-block-list =
  nonprop-keyframe-block+ ;

nonprop-keyframe-selector =
  (
    ('from' S) |
    ('to' S) |
    nonprop-percentage
  ) ;

nonprop-keyframes-name =
  (
    nonprop-custom-ident |
    nonprop-string
  ) ;

func-leader =
  (
    ('leader' S)
    ('(' S)
    nonprop-leader-type
    (')' S)
  ) ;

nonprop-leader-type =
  (
    ('dotted' S) |
    ('solid' S) |
    ('space' S) |
    nonprop-string
  ) ;

nonprop-length-percentage =
  (
    nonprop-length |
    nonprop-percentage
  ) ;

nonprop-line-name-list =
  (
    nonprop-line-names |
    nonprop-name-repeat
  )+ ;

nonprop-line-names =
  (
    ('[' S)
    nonprop-custom-ident*
    (']' S)
  ) ;

nonprop-line-style =
  (
    ('none' S) |
    ('hidden' S) |
    ('dotted' S) |
    ('dashed' S) |
    ('solid' S) |
    ('double' S) |
    ('groove' S) |
    ('ridge' S) |
    ('inset' S) |
    ('outset' S)
  ) ;

nonprop-line-width =
  (
    nonprop-length |
    ('thin' S) |
    ('medium' S) |
    ('thick' S)
  ) ;

nonprop-linear-color-hint =
  nonprop-length-percentage ;

nonprop-linear-color-stop =
  (
    nonprop-color
    nonprop-color-stop-length
  ) ;

func-linear-gradient =
  (
    ('linear-gradient' S)
    ('(' S)
    (
      nonprop-angle |
      (
        ('to' S)
        nonprop-side-or-corner
      )
    )?
    (',' S)
    nonprop-color-stop-list
    (')' S)
  ) ;

nonprop-mask-layer =
  ( (
    nonprop-mask-reference |
    (
      nonprop-position
      (
        ('/' S)
        nonprop-bg-size
      )?
    ) |
    nonprop-repeat-style |
    nonprop-geometry-box |
    (
      nonprop-geometry-box |
      ('no-clip' S)
    ) |
    nonprop-compositing-operator |
    nonprop-masking-mode
  )+ ) ;

nonprop-mask-position =
  (
    (
      nonprop-length-percentage |
      ('left' S) |
      ('center' S) |
      ('right' S)
    )
    (
      nonprop-length-percentage |
      ('top' S) |
      ('center' S) |
      ('bottom' S)
    )?
  ) ;

nonprop-mask-reference =
  (
    ('none' S) |
    nonprop-image |
    nonprop-mask-source
  ) ;

nonprop-mask-source =
  nonprop-url ;

nonprop-masking-mode =
  (
    ('alpha' S) |
    ('luminance' S) |
    ('match-source' S)
  ) ;

func-matrix =
  (
    ('matrix' S)
    ('(' S)
    nonprop-number
    (
      (
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        )
      )
    )
    (')' S)
  ) ;

func-matrix3d =
  (
    ('matrix3d' S)
    ('(' S)
    nonprop-number
    (
      (
        (
          (',' S)
          nonprop-number
        ) |
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) |
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) |
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) |
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        ) rS
        (
          (',' S)
          nonprop-number
        )
      )
    )
    (')' S)
  ) ;

func-max =
  (
    ('max' S)
    ('(' S)
    nonprop-calc-sum (
      ',' S
      nonprop-calc-sum
    )*
    (')' S)
  ) ;

nonprop-media-and =
  (
    nonprop-media-in-parens
    (
      ('and' S)
      nonprop-media-in-parens
    )+
  ) ;

nonprop-media-condition =
  (
    (
      ('not' S)?
      (
        (
          ('(' S)
          nonprop-media-condition
          (')' S)
        ) |
        nonprop-media-feature |
        nonprop-general-enclosed
      )
    ) |
    (
      (
        (
          ('(' S)
          nonprop-media-condition
          (')' S)
        ) |
        nonprop-media-feature |
        nonprop-general-enclosed
      )
      (
        (
          ('and' S) |
          ('or' S)
        )
        (
          (
            ('(' S)
            nonprop-media-condition
            (')' S)
          ) |
          nonprop-media-feature |
          nonprop-general-enclosed
        )
      )+
    )
  ) ;

nonprop-media-condition-without-or =
  (
    nonprop-media-not |
    nonprop-media-and |
    nonprop-media-in-parens
  ) ;

nonprop-media-feature =
  (
    ('(' S)
    (
      nonprop-mf-plain |
      nonprop-mf-boolean |
      nonprop-mf-range
    )
    (')' S)
  ) ;

nonprop-media-in-parens =
  (
    (
      ('(' S)
      nonprop-media-condition
      (')' S)
    ) |
    nonprop-media-feature |
    nonprop-general-enclosed
  ) ;

nonprop-media-not =
  (
    ('not' S)
    nonprop-media-in-parens
  ) ;

nonprop-media-or =
  (
    nonprop-media-in-parens
    (
      ('or' S)
      nonprop-media-in-parens
    )+
  ) ;

nonprop-media-query =
  (
    nonprop-media-condition |
    (
      (
        ('not' S) |
        ('only' S)
      )?
      nonprop-media-type
      (
        ('and' S)
        nonprop-media-condition-without-or
      )?
    )
  ) ;

nonprop-media-query-list =
  nonprop-media-query (
    ',' S
    nonprop-media-query
  )* ;

nonprop-media-type =
  nonprop-ident ;

nonprop-mf-boolean =
  nonprop-mf-name ;

nonprop-mf-name =
  nonprop-ident ;

nonprop-mf-plain =
  (
    nonprop-mf-name
    (':' S)
    nonprop-mf-value
  ) ;

nonprop-mf-range =
  (
    (
      nonprop-mf-name
      (
        ('<' S) |
        ('>' S)
      )?
      ('=' S)?
      nonprop-mf-value
    ) |
    (
      nonprop-mf-value
      (
        ('<' S) |
        ('>' S)
      )?
      ('=' S)?
      nonprop-mf-name
    ) |
    (
      nonprop-mf-value
      ('<' S)
      ('=' S)?
      nonprop-mf-name
      ('<' S)
      ('=' S)?
      nonprop-mf-value
    ) |
    (
      nonprop-mf-value
      ('>' S)
      ('=' S)?
      nonprop-mf-name
      ('>' S)
      ('=' S)?
      nonprop-mf-value
    )
  ) ;

nonprop-mf-value =
  (
    nonprop-number |
    nonprop-dimension |
    nonprop-ident |
    nonprop-ratio
  ) ;

func-min =
  (
    ('min' S)
    ('(' S)
    nonprop-calc-sum (
      ',' S
      nonprop-calc-sum
    )*
    (')' S)
  ) ;

func-minmax =
  (
    ('minmax' S)
    ('(' S)
    (
      nonprop-length |
      nonprop-percentage |
      nonprop-flex |
      ('min-content' S) |
      ('max-content' S) |
      ('auto' S)
    )
    (',' S)
    (
      nonprop-length |
      nonprop-percentage |
      nonprop-flex |
      ('min-content' S) |
      ('max-content' S) |
      ('auto' S)
    )
    (')' S)
  ) ;

nonprop-named-color =
  (
    ('transparent' S) |
    ('aliceblue' S) |
    ('antiquewhite' S) |
    ('aqua' S) |
    ('aquamarine' S) |
    ('azure' S) |
    ('beige' S) |
    ('bisque' S) |
    ('black' S) |
    ('blanchedalmond' S) |
    ('blue' S) |
    ('blueviolet' S) |
    ('brown' S) |
    ('burlywood' S) |
    ('cadetblue' S) |
    ('chartreuse' S) |
    ('chocolate' S) |
    ('coral' S) |
    ('cornflowerblue' S) |
    ('cornsilk' S) |
    ('crimson' S) |
    ('cyan' S) |
    ('darkblue' S) |
    ('darkcyan' S) |
    ('darkgoldenrod' S) |
    ('darkgray' S) |
    ('darkgreen' S) |
    ('darkgrey' S) |
    ('darkkhaki' S) |
    ('darkmagenta' S) |
    ('darkolivegreen' S) |
    ('darkorange' S) |
    ('darkorchid' S) |
    ('darkred' S) |
    ('darksalmon' S) |
    ('darkseagreen' S) |
    ('darkslateblue' S) |
    ('darkslategray' S) |
    ('darkslategrey' S) |
    ('darkturquoise' S) |
    ('darkviolet' S) |
    ('deeppink' S) |
    ('deepskyblue' S) |
    ('dimgray' S) |
    ('dimgrey' S) |
    ('dodgerblue' S) |
    ('firebrick' S) |
    ('floralwhite' S) |
    ('forestgreen' S) |
    ('fuchsia' S) |
    ('gainsboro' S) |
    ('ghostwhite' S) |
    ('gold' S) |
    ('goldenrod' S) |
    ('gray' S) |
    ('green' S) |
    ('greenyellow' S) |
    ('grey' S) |
    ('honeydew' S) |
    ('hotpink' S) |
    ('indianred' S) |
    ('indigo' S) |
    ('ivory' S) |
    ('khaki' S) |
    ('lavender' S) |
    ('lavenderblush' S) |
    ('lawngreen' S) |
    ('lemonchiffon' S) |
    ('lightblue' S) |
    ('lightcoral' S) |
    ('lightcyan' S) |
    ('lightgoldenrodyellow' S) |
    ('lightgray' S) |
    ('lightgreen' S) |
    ('lightgrey' S) |
    ('lightpink' S) |
    ('lightsalmon' S) |
    ('lightseagreen' S) |
    ('lightskyblue' S) |
    ('lightslategray' S) |
    ('lightslategrey' S) |
    ('lightsteelblue' S) |
    ('lightyellow' S) |
    ('lime' S) |
    ('limegreen' S) |
    ('linen' S) |
    ('magenta' S) |
    ('maroon' S) |
    ('mediumaquamarine' S) |
    ('mediumblue' S) |
    ('mediumorchid' S) |
    ('mediumpurple' S) |
    ('mediumseagreen' S) |
    ('mediumslateblue' S) |
    ('mediumspringgreen' S) |
    ('mediumturquoise' S) |
    ('mediumvioletred' S) |
    ('midnightblue' S) |
    ('mintcream' S) |
    ('mistyrose' S) |
    ('moccasin' S) |
    ('navajowhite' S) |
    ('navy' S) |
    ('oldlace' S) |
    ('olive' S) |
    ('olivedrab' S) |
    ('orange' S) |
    ('orangered' S) |
    ('orchid' S) |
    ('palegoldenrod' S) |
    ('palegreen' S) |
    ('paleturquoise' S) |
    ('palevioletred' S) |
    ('papayawhip' S) |
    ('peachpuff' S) |
    ('peru' S) |
    ('pink' S) |
    ('plum' S) |
    ('powderblue' S) |
    ('purple' S) |
    ('rebeccapurple' S) |
    ('red' S) |
    ('rosybrown' S) |
    ('royalblue' S) |
    ('saddlebrown' S) |
    ('salmon' S) |
    ('sandybrown' S) |
    ('seagreen' S) |
    ('seashell' S) |
    ('sienna' S) |
    ('silver' S) |
    ('skyblue' S) |
    ('slateblue' S) |
    ('slategray' S) |
    ('slategrey' S) |
    ('snow' S) |
    ('springgreen' S) |
    ('steelblue' S) |
    ('tan' S) |
    ('teal' S) |
    ('thistle' S) |
    ('tomato' S) |
    ('turquoise' S) |
    ('violet' S) |
    ('wheat' S) |
    ('white' S) |
    ('whitesmoke' S) |
    ('yellow' S) |
    ('yellowgreen' S)
  ) ;

nonprop-namespace-prefix =
  nonprop-ident ;

nonprop-ns-prefix =
  (
    (
      nonprop-ident-token |
      ('*' S)
    )?
    ('|' S)
  ) ;

nonprop-nth =
  (
    nonprop-an-plus-b |
    ('even' S) |
    ('odd' S)
  ) ;

nonprop-number-percentage =
  (
    nonprop-number |
    nonprop-percentage
  ) ;

nonprop-numeric-figure-values =
  (
    ('lining-nums' S) |
    ('oldstyle-nums' S)
  ) ;

nonprop-numeric-fraction-values =
  (
    ('diagonal-fractions' S) |
    ('stacked-fractions' S)
  ) ;

nonprop-numeric-spacing-values =
  (
    ('proportional-nums' S) |
    ('tabular-nums' S)
  ) ;

func-opacity =
  (
    ('opacity' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

nonprop-outline-radius =
  (
    nonprop-length |
    nonprop-percentage
  ) ;

nonprop-overflow-position =
  (
    ('unsafe' S) |
    ('safe' S)
  ) ;

nonprop-page-margin-box =
  (
    nonprop-page-margin-box-type
    ('{' S)
    nonprop-declaration-list
    ('}' S)
  ) ;

nonprop-page-margin-box-type =
  (
    ('@top-left-corner' S) |
    ('@top-left' S) |
    ('@top-center' S) |
    ('@top-right' S) |
    ('@top-right-corner' S) |
    ('@bottom-left-corner' S) |
    ('@bottom-left' S) |
    ('@bottom-center' S) |
    ('@bottom-right' S) |
    ('@bottom-right-corner' S) |
    ('@left-top' S) |
    ('@left-middle' S) |
    ('@left-bottom' S) |
    ('@right-top' S) |
    ('@right-middle' S) |
    ('@right-bottom' S)
  ) ;

nonprop-page-selector =
  (
    nonprop-pseudo-page+ |
    (
      nonprop-ident
      nonprop-pseudo-page*
    )
  ) ;

nonprop-page-selector-list =
  nonprop-page-selector (
    ',' S
    nonprop-page-selector
  )*? ;

func-perspective =
  (
    ('perspective' S)
    ('(' S)
    nonprop-length
    (')' S)
  ) ;

func-polygon =
  (
    ('polygon' S)
    ('(' S)
    nonprop-fill-rule?
    (',' S)
    (
      nonprop-length-percentage
      nonprop-length-percentage
    ) (
      ',' S
      (
        nonprop-length-percentage
        nonprop-length-percentage
      )
    )*
    (')' S)
  ) ;

nonprop-position =
  (
    ( (
      (
        ('left' S) |
        ('center' S) |
        ('right' S)
      ) |
      (
        ('top' S) |
        ('center' S) |
        ('bottom' S)
      )
    )+ ) |
    (
      (
        ('left' S) |
        ('center' S) |
        ('right' S) |
        nonprop-length-percentage
      )
      (
        ('top' S) |
        ('center' S) |
        ('bottom' S) |
        nonprop-length-percentage
      )?
    ) |
    (
      (
        (
          ('left' S) |
          ('right' S)
        )
        nonprop-length-percentage
      )
      (
        (
          ('top' S) |
          ('bottom' S)
        )
        nonprop-length-percentage
      )
    )
  ) ;

nonprop-pseudo-class-selector =
  (
    (
      (':' S)
      nonprop-ident-token
    ) |
    (
      (':' S)
      nonprop-function-token
      nonprop-any-value
      (')' S)
    )
  ) ;

nonprop-pseudo-element-selector =
  (
    (':' S)
    nonprop-pseudo-class-selector
  ) ;

nonprop-pseudo-page =
  (
    (':' S)
    (
      ('left' S) |
      ('right' S) |
      ('first' S) |
      ('blank' S)
    )
  ) ;

nonprop-quote =
  (
    ('open-quote' S) |
    ('close-quote' S) |
    ('no-open-quote' S) |
    ('no-close-quote' S)
  ) ;

func-radial-gradient =
  (
    ('radial-gradient' S)
    ('(' S)
    ( (
      nonprop-ending-shape |
      nonprop-size
    )+ )?
    (
      ('at' S)
      nonprop-position
    )?
    (',' S)
    nonprop-color-stop-list
    (')' S)
  ) ;

nonprop-relative-selector =
  (
    nonprop-combinator?
    nonprop-complex-selector
  ) ;

nonprop-relative-selector-list =
  nonprop-relative-selector (
    ',' S
    nonprop-relative-selector
  )* ;

nonprop-relative-size =
  (
    ('larger' S) |
    ('smaller' S)
  ) ;

nonprop-repeat-style =
  (
    ('repeat-x' S) |
    ('repeat-y' S) |
    (
      (
        (
          ('repeat' S) |
          ('space' S) |
          ('round' S) |
          ('no-repeat' S)
        ) |
        (
          ('repeat' S) |
          ('space' S) |
          ('round' S) |
          ('no-repeat' S)
        ) rS
        (
          ('repeat' S) |
          ('space' S) |
          ('round' S) |
          ('no-repeat' S)
        )
      )
    )
  ) ;

func-repeating-linear-gradient =
  (
    ('repeating-linear-gradient' S)
    ('(' S)
    (
      nonprop-angle |
      (
        ('to' S)
        nonprop-side-or-corner
      )
    )?
    (',' S)
    nonprop-color-stop-list
    (')' S)
  ) ;

func-repeating-radial-gradient =
  (
    ('repeating-radial-gradient' S)
    ('(' S)
    ( (
      nonprop-ending-shape |
      nonprop-size
    )+ )?
    (
      ('at' S)
      nonprop-position
    )?
    (',' S)
    nonprop-color-stop-list
    (')' S)
  ) ;

func-rgb =
  (
    (
      ('rgb' S)
      ('(' S)
      (
        (
          nonprop-percentage rS
          nonprop-percentage rS
          nonprop-percentage
        )
      )
      (
        ('/' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('rgb' S)
      ('(' S)
      (
        (
          nonprop-number rS
          nonprop-number rS
          nonprop-number
        )
      )
      (
        ('/' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('rgb' S)
      ('(' S)
      (
        (
          nonprop-percentage ',' S
          nonprop-percentage ',' S
          nonprop-percentage
        )
      )
      (
        (',' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('rgb' S)
      ('(' S)
      (
        (
          nonprop-number ',' S
          nonprop-number ',' S
          nonprop-number
        )
      )
      (
        (',' S)
        nonprop-alpha-value
      )?
      (')' S)
    )
  ) ;

func-rgba =
  (
    (
      ('rgba' S)
      ('(' S)
      (
        (
          nonprop-percentage rS
          nonprop-percentage rS
          nonprop-percentage
        )
      )
      (
        ('/' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('rgba' S)
      ('(' S)
      (
        (
          nonprop-number rS
          nonprop-number rS
          nonprop-number
        )
      )
      (
        ('/' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('rgba' S)
      ('(' S)
      (
        (
          nonprop-percentage ',' S
          nonprop-percentage ',' S
          nonprop-percentage
        )
      )
      (
        (',' S)
        nonprop-alpha-value
      )?
      (')' S)
    ) |
    (
      ('rgba' S)
      ('(' S)
      (
        (
          nonprop-number ',' S
          nonprop-number ',' S
          nonprop-number
        )
      )
      (
        (',' S)
        nonprop-alpha-value
      )?
      (')' S)
    )
  ) ;

func-rotate =
  (
    ('rotate' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

func-rotate3d =
  (
    ('rotate3d' S)
    ('(' S)
    nonprop-number
    (',' S)
    nonprop-number
    (',' S)
    nonprop-number
    (',' S)
    nonprop-angle
    (')' S)
  ) ;

func-rotateX =
  (
    ('rotateX' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

func-rotateY =
  (
    ('rotateY' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

func-rotateZ =
  (
    ('rotateZ' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

func-saturate =
  (
    ('saturate' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

func-scale =
  (
    ('scale' S)
    ('(' S)
    nonprop-number
    (
      (',' S)
      nonprop-number
    )?
    (')' S)
  ) ;

func-scale3d =
  (
    ('scale3d' S)
    ('(' S)
    nonprop-number
    (',' S)
    nonprop-number
    (',' S)
    nonprop-number
    (')' S)
  ) ;

func-scaleX =
  (
    ('scaleX' S)
    ('(' S)
    nonprop-number
    (')' S)
  ) ;

func-scaleY =
  (
    ('scaleY' S)
    ('(' S)
    nonprop-number
    (')' S)
  ) ;

func-scaleZ =
  (
    ('scaleZ' S)
    ('(' S)
    nonprop-number
    (')' S)
  ) ;

nonprop-self-position =
  (
    ('center' S) |
    ('start' S) |
    ('end' S) |
    ('self-start' S) |
    ('self-end' S) |
    ('flex-start' S) |
    ('flex-end' S)
  ) ;

func-sepia =
  (
    ('sepia' S)
    ('(' S)
    nonprop-number-percentage
    (')' S)
  ) ;

nonprop-shadow =
  (
    ('inset' S)?
    (
      (
        nonprop-length rS
        nonprop-length |
        nonprop-length rS
        nonprop-length rS
        nonprop-length |
        nonprop-length rS
        nonprop-length rS
        nonprop-length rS
        nonprop-length
      )
    )
    nonprop-color?
  ) ;

nonprop-shadow-t =
  (
    (
      (
        nonprop-length rS
        nonprop-length |
        nonprop-length rS
        nonprop-length rS
        nonprop-length
      )
    )
    nonprop-color?
  ) ;

nonprop-shape =
  (
    (
      ('rect' S)
      ('(' S)
      nonprop-top
      ',' S
      nonprop-right
      (',' S)
      nonprop-bottom
      (',' S)
      nonprop-left
      (')' S)
    ) |
    (
      ('rect' S)
      ('(' S)
      nonprop-top
      nonprop-right
      nonprop-bottom
      nonprop-left
      (')' S)
    )
  ) ;

nonprop-shape-box =
  (
    nonprop-box |
    ('margin-box' S)
  ) ;

nonprop-shape-radius =
  (
    nonprop-length-percentage |
    ('closest-side' S) |
    ('farthest-side' S)
  ) ;

nonprop-side-or-corner =
  ( (
    (
      ('left' S) |
      ('right' S)
    ) |
    (
      ('top' S) |
      ('bottom' S)
    )
  )+ ) ;

nonprop-single-animation =
  ( (
    nonprop-time |
    nonprop-timing-function |
    nonprop-time |
    nonprop-single-animation-iteration-count |
    nonprop-single-animation-direction |
    nonprop-single-animation-fill-mode |
    nonprop-single-animation-play-state |
    (
      ('none' S) |
      nonprop-keyframes-name
    )
  )+ ) ;

nonprop-single-animation-direction =
  (
    ('normal' S) |
    ('reverse' S) |
    ('alternate' S) |
    ('alternate-reverse' S)
  ) ;

nonprop-single-animation-fill-mode =
  (
    ('none' S) |
    ('forwards' S) |
    ('backwards' S) |
    ('both' S)
  ) ;

nonprop-single-animation-iteration-count =
  (
    ('infinite' S) |
    nonprop-number
  ) ;

nonprop-single-animation-play-state =
  (
    ('running' S) |
    ('paused' S)
  ) ;

nonprop-single-transition =
  ( (
    (
      ('none' S) |
      nonprop-single-transition-property
    ) |
    nonprop-time |
    nonprop-timing-function |
    nonprop-time
  )+ ) ;

nonprop-single-transition-property =
  (
    ('all' S) |
    nonprop-custom-ident
  ) ;

nonprop-size =
  (
    ('closest-side' S) |
    ('farthest-side' S) |
    ('closest-corner' S) |
    ('farthest-corner' S) |
    nonprop-length |
    (
      (
        nonprop-length-percentage rS
        nonprop-length-percentage
      )
    )
  ) ;

func-skew =
  (
    ('skew' S)
    ('(' S)
    nonprop-angle
    (
      (',' S)
      nonprop-angle
    )?
    (')' S)
  ) ;

func-skewX =
  (
    ('skewX' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

func-skewY =
  (
    ('skewY' S)
    ('(' S)
    nonprop-angle
    (')' S)
  ) ;

nonprop-src =
  (
    (
      nonprop-url
      (
        ('format' S)
        ('(' S)
        nonprop-string (
          ',' S
          nonprop-string
        )*
        (')' S)
      )?
    ) |
    (
      ('local' S)
      ('(' S)
      nonprop-family-name
      (')' S)
    )
  ) (
    ',' S
    (
      (
        nonprop-url
        (
          ('format' S)
          ('(' S)
          nonprop-string (
            ',' S
            nonprop-string
          )*
          (')' S)
        )?
      ) |
      (
        ('local' S)
        ('(' S)
        nonprop-family-name
        (')' S)
      )
    )
  )* ;

nonprop-step-position =
  (
    ('jump-start' S) |
    ('jump-end' S) |
    ('jump-none' S) |
    ('jump-both' S) |
    ('start' S) |
    ('end' S)
  ) ;

nonprop-step-timing-function =
  (
    ('step-start' S) |
    ('step-end' S) |
    (
      ('steps' S)
      ('(' S)
      nonprop-integer
      (
        (',' S)
        nonprop-step-position
      )?
      (')' S)
    )
  ) ;

nonprop-subclass-selector =
  (
    nonprop-id-selector |
    nonprop-class-selector |
    nonprop-attribute-selector |
    nonprop-pseudo-class-selector
  ) ;

nonprop-supports-condition =
  (
    (
      ('not' S)
      (
        (
          ('(' S)
          nonprop-supports-condition
          (')' S)
        ) |
        nonprop-supports-feature |
        nonprop-general-enclosed
      )
    ) |
    (
      (
        (
          ('(' S)
          nonprop-supports-condition
          (')' S)
        ) |
        nonprop-supports-feature |
        nonprop-general-enclosed
      )
      (
        ('and' S)
        (
          (
            ('(' S)
            nonprop-supports-condition
            (')' S)
          ) |
          nonprop-supports-feature |
          nonprop-general-enclosed
        )
      )*
    ) |
    (
      (
        (
          ('(' S)
          nonprop-supports-condition
          (')' S)
        ) |
        nonprop-supports-feature |
        nonprop-general-enclosed
      )
      (
        ('or' S)
        (
          (
            ('(' S)
            nonprop-supports-condition
            (')' S)
          ) |
          nonprop-supports-feature |
          nonprop-general-enclosed
        )
      )*
    )
  ) ;

nonprop-supports-decl =
  (
    ('(' S)
    nonprop-declaration
    (')' S)
  ) ;

nonprop-supports-feature =
  (
    nonprop-supports-decl |
    nonprop-supports-selector-fn
  ) ;

nonprop-supports-in-parens =
  (
    (
      ('(' S)
      nonprop-supports-condition
      (')' S)
    ) |
    nonprop-supports-feature |
    nonprop-general-enclosed
  ) ;

nonprop-supports-selector-fn =
  (
    ('selector' S)
    ('(' S)
    nonprop-complex-selector
    (')' S)
  ) ;

nonprop-symbol =
  (
    nonprop-string |
    nonprop-image |
    nonprop-custom-ident
  ) ;

nonprop-target =
  (
    func-target-counter |
    func-target-counters |
    func-target-text
  ) ;

func-target-counter =
  (
    ('target-counter' S)
    ('(' S)
    (
      nonprop-string |
      nonprop-url
    )
    (',' S)
    nonprop-custom-ident
    (',' S)
    nonprop-counter-style?
    (')' S)
  ) ;

func-target-counters =
  (
    ('target-counters' S)
    ('(' S)
    (
      nonprop-string |
      nonprop-url
    )
    (',' S)
    nonprop-custom-ident
    (',' S)
    nonprop-string
    (',' S)
    nonprop-counter-style?
    (')' S)
  ) ;

func-target-text =
  (
    ('target-text' S)
    ('(' S)
    (
      nonprop-string |
      nonprop-url
    )
    (',' S)
    (
      ('content' S) |
      ('before' S) |
      ('after' S) |
      ('first-letter' S)
    )?
    (')' S)
  ) ;

nonprop-time-percentage =
  (
    nonprop-time |
    nonprop-percentage
  ) ;

nonprop-timing-function =
  (
    ('linear' S) |
    nonprop-cubic-bezier-timing-function |
    nonprop-step-timing-function
  ) ;

nonprop-track-breadth =
  (
    nonprop-length-percentage |
    nonprop-flex |
    ('min-content' S) |
    ('max-content' S) |
    ('auto' S)
  ) ;

nonprop-track-list =
  (
    (
      nonprop-line-names?
      (
        nonprop-track-size |
        nonprop-track-repeat
      )
    )+
    nonprop-line-names?
  ) ;

nonprop-track-repeat =
  (
    ('repeat' S)
    ('(' S)
    nonprop-positive-integer
    (',' S)
    (
      nonprop-line-names?
      nonprop-track-size
    )+
    nonprop-line-names?
    (')' S)
  ) ;

nonprop-track-size =
  (
    nonprop-track-breadth |
    (
      ('minmax' S)
      ('(' S)
      nonprop-inflexible-breadth
      (',' S)
      nonprop-track-breadth
      (')' S)
    ) |
    (
      ('fit-content' S)
      ('(' S)
      (
        nonprop-length |
        nonprop-percentage
      )
      (')' S)
    )
  ) ;

nonprop-transform-function =
  (
    func-matrix |
    func-translate |
    func-translateX |
    func-translateY |
    func-scale |
    func-scaleX |
    func-scaleY |
    func-rotate |
    func-skew |
    func-skewX |
    func-skewY |
    func-matrix3d |
    func-translate3d |
    func-translateZ |
    func-scale3d |
    func-scaleZ |
    func-rotate3d |
    func-rotateX |
    func-rotateY |
    func-rotateZ |
    func-perspective
  ) ;

nonprop-transform-list =
  nonprop-transform-function+ ;

func-translate =
  (
    ('translate' S)
    ('(' S)
    nonprop-length-percentage
    (
      (',' S)
      nonprop-length-percentage
    )?
    (')' S)
  ) ;

func-translate3d =
  (
    ('translate3d' S)
    ('(' S)
    nonprop-length-percentage
    (',' S)
    nonprop-length-percentage
    (',' S)
    nonprop-length
    (')' S)
  ) ;

func-translateX =
  (
    ('translateX' S)
    ('(' S)
    nonprop-length-percentage
    (')' S)
  ) ;

func-translateY =
  (
    ('translateY' S)
    ('(' S)
    nonprop-length-percentage
    (')' S)
  ) ;

func-translateZ =
  (
    ('translateZ' S)
    ('(' S)
    nonprop-length
    (')' S)
  ) ;

nonprop-type-or-unit =
  (
    ('string' S) |
    ('integer' S) |
    ('color' S) |
    ('url' S) |
    ('integer' S) |
    ('number' S) |
    ('length' S) |
    ('angle' S) |
    ('time' S) |
    ('frequency' S) |
    ('em' S) |
    ('ex' S) |
    ('px' S) |
    ('rem' S) |
    ('vw' S) |
    ('vh' S) |
    ('vmin' S) |
    ('vmax' S) |
    ('mm' S) |
    ('q' S) |
    ('cm' S) |
    ('in' S) |
    ('pt' S) |
    ('pc' S) |
    ('deg' S) |
    ('grad' S) |
    ('rad' S) |
    ('ms' S) |
    ('s' S) |
    ('Hz' S) |
    ('kHz' S) |
    ('%' S)
  ) ;

nonprop-type-selector =
  (
    nonprop-wq-name |
    (
      nonprop-ns-prefix?
      ('*' S)
    )
  ) ;

func-var =
  (
    ('var' S)
    ('(' S)
    nonprop-custom-property-name
    (',' S)
    nonprop-declaration-value?
    (')' S)
  ) ;

nonprop-viewport-length =
  (
    ('auto' S) |
    nonprop-length-percentage
  ) ;

nonprop-wq-name =
  (
    nonprop-ns-prefix?
    nonprop-ident-token
  )

(* Base CSS Types: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Types *)

nonprop-positive-integer = positive-integer ;

nonprop-angle      = any-number ( 'deg' | 'grad' | 'rad' | 'turn' ) ;
nonprop-flex       = any-number 'fr' S ;
nonprop-frequency  = any-number ( 'Hz' | 'kHz' ) S ;
nonprop-integer    = integer ;
nonprop-length     = any-number length-unit S
                   | '0' S ;
nonprop-number     = any-number S ;
nonprop-percentage = any-number '%' S ;
nonprop-resolution = any-number ( 'dpi' | 'dpcm' | 'dppx' | 'px' ) S ;
nonprop-time       = any-number ( 's' | 'ms' ) S ;

nonprop-ratio      = positive-integer '/' positive-integer ;

nonprop-string = quoted-string ;

(* https://developer.mozilla.org/en-US/docs/Web/CSS/custom-ident *)
nonprop-custom-ident = #'(?:[A-Za-z_]|-[A-Za-z_]|\\[0-9A-F]{1,6})(?:[A-Za-z0-9_-]|\\[0-9A-F]{1,6})*' S ;

(* TODO: improve urls: https://developer.mozilla.org/en-US/docs/Web/CSS/url *)
(*
nonprop-url = ( 'url(' url ')' S | 'url("' url '")' S )
            / 'url(' quoted-string ')' S;
*)
nonprop-url = 'url(' quoted-string ')' S;


nonprop-unicode-range = #'U[+](?:[0-9A-Fa-f]{1,4}-[0-9A-Fa-f]{1,4}|[0-9A-Fa-f?]{1,4})'
                        ( ',' S nonprop-unicode-range )* ;

nonprop-ident-token = "STUB_ident_token" ;
nonprop-declaration = css-declaration ;

nonprop-charset = charset ;

(* non-standard needed by font-face at-rules *)
(* TODO: are these bugs? *)

nonprop-font-variant = prop-font-variant ;
nonprop-font-feature-settings = prop-font-feature-settings ;
nonprop-font-variation-settings = prop-unknown ;
nonprop-font-stretch = prop-font-stretch ;
nonprop-font-weight = prop-font-weight ;
nonprop-font-style = prop-font-style ;


(* standard nonprop -> prop mappings *)
(* TODO: are these bugs? *)

nonprop-top = prop-top ;
nonprop-bottom = prop-bottom ;
nonprop-left = prop-left ;
nonprop-right = prop-right ;


(* Other Misc Missing *)
(* TODO: figure out why these are missing. *)

nonprop-hex-color = #'#[0-9A-Fa-f]{3}' S
                  | #'#[0-9A-Fa-f]{6}' S ;

nonprop-id-selector = nonprop-custom-ident ;
nonprop-ident = nonprop-custom-ident ;
nonprop-dimension = nonprop-resolution ;

func-path = "STUB_func_path" ;
nonprop-an-plus-b = "STUB_an_plus_b" ;
nonprop-any-value = "STUB_any_value" ;
nonprop-attr-name = "STUB_attr_name" ;
nonprop-attr-fallback = "STUB_attr_fallback" ;
nonprop-clip-style = "STUB_clip_style" ;
nonprop-custom-property-name = "STUB_custom_property" ;
nonprop-declaration-value = "STUB_declaration_value" ;
nonprop-function-token = "STUB_flex_function_token" ;
nonprop-mask-attachment = "STUB_mask_attachment" ;
nonprop-name-repeat = "STUB_name_repeat" ;
nonprop-string-token = "STUB_string_token" ;
nonprop-x = "11" ;
nonprop-y = "11" ;


(* TODO: from at-rule descriptors *)

nonprop-counter-system = "STUB_counter_system" ;
nonprop-counter-symbols = "STUB_counter_symbols" ;
nonprop-additive-symbols = "STUB_additive_symbols" ;
nonprop-negative-symbol = "STUB_negative_symbol" ;
nonprop-prefix = "STUB_prefix" ;
nonprop-suffix = "STUB_suffix" ;
nonprop-range = "STUB_range" ;
nonprop-padding = "STUB_padding" ;
nonprop-speak-as = "STUB_speak_as" ;
nonprop-page-body = "STUB_page_body" ;



(* with * inside, "S rS" breaks, with *? nothing is matched *)
S          = #'\s'* ;
rS         = #'\s+' ;

integer = #"-?[0-9]+" ;
non-negative-integer = #"[0-9]+" ;
positive-integer = #"[1-9][0-9]*" ;
floating-point-number = #"-?[0-9]*[.][0-9]+(?:[eE]-?[0-9]+)?" ;
any-number = integer | floating-point-number ;

(* TODO: improve strings: https://developer.mozilla.org/en-US/docs/Web/CSS/string *)
quoted-string = #"'[\"A-Za-z0-9 .,_:/%=#?+\-^]*'" S
              | #"\"['A-Za-z0-9 .,_:/%=#?+\-^]*\"" S ;


(* HTML *)
mime-type = #'[a-z]+/[a-z0-9+-]+'
lang = #'[a-z]+(?:-[A-Za-z]+)?'

(* CSS *)
length-unit = 'em' | 'ex' | 'ch' | 'ic' | 'rem' | 'lh' | 'rlh'
            | 'vh' | 'vw' | 'vi' | 'vb' | 'vmin' | 'vmax'
            | 'px' | 'mm' | 'q' | 'cm' | 'in' |  'pt' | 'pc' | 'mozmm' ;

url = #'[A-Za-z0-9$_@.,&+%=;/#?:-]*[A-Za-z0-9$_@.&+%=;/#?:-]'


charset = 'UTF-8'
        | 'utf-8'
        | 'iso-8859-15'
